# Hackathon optimization list — full-corpus sweep, 2026-08-27

Source: the optimizer's 2026-08-27 sweep (2,954 Spark runs scanned, 1,620 with findings, 1,334 clean). One line per run in the sweep backlog, so each pair below is built from the run's TOP finding only; a run's lower-ranked findings are not counted, which makes every per-pair total a floor.

**Totals: 67 distinct (job, mechanism) pairs. 30,163 executor-hours at stake** (executor-hours are directly measured only for the idle-executor mechanism; spill, shuffle, straggler, and skew pairs are quantified in GiB and task-time ratios below). **Gates: 8 PR-READY, 53 VERIFY-FIRST, 6 already triaged in the implementation queue** (`tickets/audi_1194_optimizer_efficiency_crawler/artifacts/audi_1194_implementation_queue.md`, referenced as QUEUE #N, not duplicated).

Mechanism key (from the detector taxonomy in `airflow_optimizer/optimizations.py`):
- `idle_reserved_executors`: executors held but mostly idle; the billed allocation dwarfs the work done.
- `disk_spill`: per-task data exceeds execution memory, so the stage writes intermediate data to disk.
- `shuffle_partition_sizing`: a large shuffle divided into too few partitions, so each reducer is oversized and spills.
- `straggler`: one task runs many times longer than the median with uniform input data; a slow node or IO stall, not skew.
- `skew`: the slow task also reads far more data than the median; one key holds most of the data.
- `shuffle_fetch_wait`: tasks stall waiting to fetch shuffle blocks instead of computing.

Gate meanings: PR-READY = pure config, mechanism clear from the log. VERIFY-FIRST = an event-log check must pick the number or confirm the mechanism (named per row). OWNER = needs the owning team's answer (question named). QUEUE #N = already triaged; see the implementation queue.

Traps that bind every fix here:
- Builder config silently beats decorator config; confirm where the job's Spark conf is set before editing.
- `spark.speculation` must stay `false` on any GCS-writing stage (speculative duplicates race the ManifestCommitter).
- Any decorator `runtime_properties` change requires regenerating `dags/model_task_config.json` via `MNTN_SDLC_ENV=dev python model_upload.py --dryrun`.

## Ranked pairs (executor-hours where measured, then run count)

| # | Job | Mechanism | Measured evidence (this sweep) | Fix | Gate |
|---|---|---|---|---|---|
| 1 | `site_network_hourly` | idle_reserved_executors | 239 runs, 22,230 idle exec-h summed (avg ~93/run) | see implementation queue | QUEUE #1 (merged airflow-ti#1232, measuring from next sweep) |
| 2 | `aug_log_ip_hourly` | idle_reserved_executors | 126 runs, 4,086 idle exec-h summed (avg ~32/run) | see implementation queue | QUEUE #7 (apply the #1 idle-tail mechanism once it proves out) |
| 3 | `aug_log_ip_vertical_id_hourly` | idle_reserved_executors | 114 runs, 3,847 idle exec-h summed (avg ~34/run) | see implementation queue | QUEUE #7 (apply the #1 idle-tail mechanism once it proves out) |
| 4 | `aug_log_ip_vertical_id_hourly` | shuffle_fetch_wait | 306 runs, stage 11 spends 35% median (51% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 11's map output sits on few executors (map ran during scale-up) |
| 5 | `site_network_hourly` | shuffle_fetch_wait | 53 runs, stage 9 spends 52% median (79% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 9's map output sits on few executors (map ran during scale-up) |
| 6 | `aug_log_ip_hourly` | straggler | 34 runs, stage 2 slowest task 8x median (max 26x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 2 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 7 | `intent_score_map` | disk_spill | 24 runs, stage 2 spills 2,448 GiB median / 3,385 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 2's current partition count and whether the spill is shuffle-side, then size the value |
| 8 | `ipdsc_ds_49` | disk_spill | 24 runs, stage 1 spills 16 GiB median / 18 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 1's current partition count and whether the spill is shuffle-side, then size the value |
| 9 | `ipdsc_ds_35` | straggler | 23 runs, stage 2 slowest task 76x median (max 186x) on uniform data | see implementation queue | QUEUE #4 OWNER: speculation reverted by gauntlet; ask what straggler fix is safe for GCS overwrite writes |
| 10 | `guid_log_pivot_ip_vertical_id` | disk_spill | 23 runs, stage 33 spills 19 GiB median / 145 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 33's current partition count and whether the spill is shuffle-side, then size the value |
| 11 | `guid_conv_log_pivot_ip_vertical_id` | disk_spill | 23 runs, stage 34 spills 19 GiB median / 132 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 34's current partition count and whether the spill is shuffle-side, then size the value |
| 12 | `conv_log_derived_ip` | disk_spill | 23 runs, stage 1 spills 6 GiB median / 10 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 1's current partition count and whether the spill is shuffle-side, then size the value |
| 13 | `ipdsc_ds_67` | disk_spill | 22 runs, stage 3 spills 81 GiB median / 84 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 3's current partition count and whether the spill is shuffle-side, then size the value |
| 14 | `ipdsc_ds_2` | disk_spill | 22 runs, stage 1 spills 56 GiB median / 92 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 1's current partition count and whether the spill is shuffle-side, then size the value |
| 15 | `fangorn_prospecting_scoring` | disk_spill | 22 runs, stage 13 spills 4,288 GiB median / 4,394 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 13's current partition count and whether the spill is shuffle-side, then size the value |
| 16 | `advertiser_mid` | shuffle_fetch_wait | 22 runs, stage 24 spends 70% median (82% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 24's map output sits on few executors (map ran during scale-up) |
| 17 | `advertiser_score_distribution_monitor` | shuffle_partition_sizing | 22 runs, stage 1 shuffle 208 GiB median at ~1,660 MiB/partition | set spark.sql.shuffle.partitions ~916 (~256 MiB per partition) | PR-READY |
| 18 | `ipdsc_ds_17` | disk_spill | 22 runs, stage 4 spills 33 GiB median / 41 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 4's current partition count and whether the spill is shuffle-side, then size the value |
| 19 | `ipdsc_46_monitor` | disk_spill | 22 runs, stage 10 spills 11 GiB median / 11 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 10's current partition count and whether the spill is shuffle-side, then size the value |
| 20 | `ipdsc_14_monitor` | disk_spill | 22 runs, stage 10 spills 6 GiB median / 7 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 10's current partition count and whether the spill is shuffle-side, then size the value |
| 21 | `ipdsc_49_monitor` | disk_spill | 22 runs, stage 10 spills 6 GiB median / 6 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 10's current partition count and whether the spill is shuffle-side, then size the value |
| 22 | `ipdsc_ds_13` | disk_spill | 22 runs, stage 1 spills 31 GiB median / 33 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 1's current partition count and whether the spill is shuffle-side, then size the value |
| 23 | `conversion_log_advertiser_id_dsc_id` | shuffle_partition_sizing | 21 runs, stage 5 shuffle 840 GiB median at ~860 MiB/partition | set spark.sql.shuffle.partitions ~3508 (~256 MiB per partition) | PR-READY |
| 24 | `site_visit_signal_advertiser_id_dsc_id` | shuffle_partition_sizing | 21 runs, stage 4 shuffle 814 GiB median at ~834 MiB/partition | set spark.sql.shuffle.partitions ~3392 (~256 MiB per partition) | PR-READY |
| 25 | `guid_log_advertiser_id_dsc_id` | shuffle_partition_sizing | 21 runs, stage 5 shuffle 816 GiB median at ~836 MiB/partition | set spark.sql.shuffle.partitions ~3400 (~256 MiB per partition) | PR-READY |
| 26 | `intent_score_household_map` | shuffle_partition_sizing | 21 runs, stage 10 shuffle 3,702 GiB median at ~3,791 MiB/partition | set spark.sql.shuffle.partitions ~15040 (~256 MiB per partition) | PR-READY |
| 27 | `audience_intent_scoring_staging_ds46` | disk_spill | 21 runs, stage 20 spills 2,213 GiB median / 2,216 GiB max to disk | see implementation queue | QUEUE #5 (ready once the two-DAG name ambiguity is pinned to one DAG) |
| 28 | `fangorn_predictions_vertical` | disk_spill | 21 runs, stage 2 spills 300 GiB median / 306 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 2's current partition count and whether the spill is shuffle-side, then size the value |
| 29 | `ipdsc_42_monitor` | shuffle_fetch_wait | 20 runs, stage 18 spends 64% median (79% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 18's map output sits on few executors (map ran during scale-up) |
| 30 | `tpa_export_enrich` | shuffle_fetch_wait | 20 runs, stage 6 spends 71% median (75% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 6's map output sits on few executors (map ran during scale-up) |
| 31 | `vertical_size_monitor` | disk_spill | 20 runs, stage 13 spills 16 GiB median / 16 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 13's current partition count and whether the spill is shuffle-side, then size the value |
| 32 | `conv_log_ip_advertiser_id` | skew | 16 runs, stage 13 skewed 38x median (max 65x), skewed key holds most of the data | spark.sql.adaptive.skewJoin.enabled=true if a join; salt the key otherwise | VERIFY-FIRST: confirm stage 13 is a join and identify the skewed key from the event log |
| 33 | `ipdsc_ds_47` | disk_spill | 15 runs, stage 5 spills 39 GiB median / 44 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 5's current partition count and whether the spill is shuffle-side, then size the value |
| 34 | `guid_log_ip_guid_advertiser_id` | skew | 14 runs, stage 9 skewed 17x median (max 20x), skewed key holds most of the data | spark.sql.adaptive.skewJoin.enabled=true if a join; salt the key otherwise | VERIFY-FIRST: confirm stage 9 is a join and identify the skewed key from the event log |
| 35 | `fangorn_score_monitor` | disk_spill | 13 runs, stage 17 spills 1,130 GiB median / 1,153 GiB max to disk | see implementation queue | QUEUE #3 (merged airflow-ti#1231, measuring from next sweep) |
| 36 | `ipdsc_third_party_audience_builder` | shuffle_partition_sizing | 12 runs, stage 4 shuffle 545 GiB median at ~1,089 MiB/partition | set spark.sql.shuffle.partitions ~2240 (~256 MiB per partition) | PR-READY |
| 37 | `advertiser_join` | straggler | 12 runs, stage 3 slowest task 13x median (max 18x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 3 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 38 | `ipdsc_ds_46` | shuffle_fetch_wait | 12 runs, stage 3 spends 52% median (59% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 3's map output sits on few executors (map ran during scale-up) |
| 39 | `prospecting_join` | shuffle_partition_sizing | 11 runs, stage 57 shuffle 10,623 GiB median at ~544 MiB/partition | set spark.sql.shuffle.partitions ~42988 (~256 MiB per partition) | PR-READY |
| 40 | `identity_targeted_signal` | straggler | 11 runs, stage 3 slowest task 6x median (max 18x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 3 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 41 | `advertiser_join` | disk_spill | 10 runs, stage 3 spills 1,669 GiB median / 1,726 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 3's current partition count and whether the spill is shuffle-side, then size the value |
| 42 | `tpa_mntn_id_export` | shuffle_fetch_wait | 9 runs, stage 398 spends 40% median (54% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 398's map output sits on few executors (map ran during scale-up) |
| 43 | `fangorn_score_monitor` | straggler | 7 runs, stage 12 slowest task 15x median (max 22x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 12 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 44 | `ipdsc_ds_47` | straggler | 7 runs, stage 2 slowest task 14x median (max 38x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 2 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 45 | `aug_log_ip` | disk_spill | 7 runs, stage 1 spills 1 GiB median / 2 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 1's current partition count and whether the spill is shuffle-side, then size the value |
| 46 | `audience_intent_scoring_staging_ds46` | shuffle_fetch_wait | 6 runs, stage 8 spends 69% median (70% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 8's map output sits on few executors (map ran during scale-up) |
| 47 | `prospecting_join` | straggler | 5 runs, stage 41 slowest task 14x median (max 18x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 41 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 48 | `aug_log_ip_vertical_id_hourly` | straggler | 5 runs, stage 7 slowest task 35x median (max 57x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 7 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 49 | `site_network_hourly` | straggler | 4 runs, stage 9 slowest task 41x median (max 47x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 9 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 50 | `fangorn_household_predictions_vertical` | disk_spill | 4 runs, stage 1 spills 4 GiB median / 4 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 1's current partition count and whether the spill is shuffle-side, then size the value |
| 51 | `ipdsc_42_monitor` | skew | 3 runs, stage 18 skewed 11x median (max 13x), skewed key holds most of the data | spark.sql.adaptive.skewJoin.enabled=true if a join; salt the key otherwise | VERIFY-FIRST: confirm stage 18 is a join and identify the skewed key from the event log |
| 52 | `guid_log_ip_advertiser_id` | skew | 2 runs, stage 9 skewed 12x median (max 12x), skewed key holds most of the data | spark.sql.adaptive.skewJoin.enabled=true if a join; salt the key otherwise | VERIFY-FIRST: confirm stage 9 is a join and identify the skewed key from the event log |
| 53 | `ipdsc_ds_14` | disk_spill | 2 runs, stage 1 spills 3 GiB median / 3 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 1's current partition count and whether the spill is shuffle-side, then size the value |
| 54 | `aug_log_ip_hourly` | shuffle_fetch_wait | 2 runs, stage 4 spends 38% median (38% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 4's map output sits on few executors (map ran during scale-up) |
| 55 | `intent_score_household_map` | straggler | 1 runs, stage 8 slowest task 15x median (max 15x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 8 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 56 | `site_visit_signal_advertiser_id_dsc_id` | straggler | 1 runs, stage 4 slowest task 17x median (max 17x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 4 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 57 | `guid_log_advertiser_id_dsc_id` | disk_spill | 1 runs, stage 13 spills 347 GiB median / 347 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 13's current partition count and whether the spill is shuffle-side, then size the value |
| 58 | `vertical_size_monitor` | shuffle_fetch_wait | 1 runs, stage 13 spends 73% median (73% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 13's map output sits on few executors (map ran during scale-up) |
| 59 | `guid_log_derived_household_id_vertical_id` | shuffle_fetch_wait | 1 runs, stage 11 spends 74% median (74% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 11's map output sits on few executors (map ran during scale-up) |
| 60 | `advertiser_high` | straggler | 1 runs, stage 3 slowest task 19x median (max 19x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 3 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 61 | `ipdsc_ds_63` | straggler | 1 runs, stage 2 slowest task 453x median (max 453x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 2 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 62 | `hhdsc_ds_19` | straggler | 1 runs, stage 23 slowest task 26x median (max 26x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 23 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 63 | `household_score_distribution_monitor` | shuffle_partition_sizing | 1 runs, stage 6 shuffle 2,224 GiB median at ~4,448 MiB/partition | set spark.sql.shuffle.partitions ~8896 (~256 MiB per partition) | PR-READY |
| 64 | `tpa_mntn_id_export` | straggler | 1 runs, stage 778 slowest task 60x median (max 60x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 778 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |
| 65 | `guid_log_pivot_household_id_vertical_id` | disk_spill | 1 runs, stage 33 spills 10 GiB median / 10 GiB max to disk | raise spark.sql.shuffle.partitions for the spilling stage (target ~256 MiB in-memory per task); executor memory only if spill persists | VERIFY-FIRST: read the event log for stage 33's current partition count and whether the spill is shuffle-side, then size the value |
| 66 | `site_visit_signal_derived_advertiser_id_dsc_id` | shuffle_fetch_wait | 1 runs, stage 19 spends 46% median (46% max) of task time on shuffle-fetch wait | raise spark.dynamicAllocation.initialExecutors so the map stage spreads its output; raising shuffle partitions makes it worse | VERIFY-FIRST: check whether stage 19's map output sits on few executors (map ran during scale-up) |
| 67 | `ipdsc_ds_42` | straggler | 1 runs, stage 3 slowest task 7x median (max 7x) on uniform data | spark.speculation=true with spark.speculation.quantile=0.9 | VERIFY-FIRST: confirm stage 3 is compute, not a GCS write (speculation races the ManifestCommitter on GCS writers; must stay false there) |

## Unprofiled DAGs

This sweep's coverage pass could not enumerate the DAG bundle (`No module named 'airflow'`), so completeness of the backlog is unknown for 2026-08-27. The 2026-08-26 enumeration (65 active DAGs, 30 with a Spark task) is the latest complete picture of what stays unprofiled and why:

- 17 Spark DAGs produced no event log in the window (did not run, or the log had not landed): conversion_signal_backfill_workflow, crm_match_rate, feature_store_snapshot, fpa_site_visit_batch_serverless, gcp_mntn_global_data, gcp_tpa_daily_metrics, guid_geos_summary_to_integration, the five hashed_email_* signal DAGs, hashed_phone_backfill_workflow, hhdsc_build, materialize_mntn_first_party, pixel_page_view_signal_backfill_workflow, run_test_models.
- 16 DAGs run Spark on engines whose logs are unreachable: Databricks job clusters with no cluster_log_conf (create_ip_verticals, fetch_common_crawl, keyword_ddp_reporting, missing_domains, the mntn_match_* family, vertical_classification_api, vertical_correlation_pipeline, databricks_guid_geos) and managed clusters with no spark.eventLog.dir (advertiser_scores_monitor, part of run_test_models).
- 35 DAGs have no Spark task at all (Python/@task, BigQuery, GKE, sensor, or export operators); nothing to profile.

Full lists: `tickets/audi_1194_optimizer_efficiency_crawler/outputs/optimizer_coverage_2026-08-26.md`.
