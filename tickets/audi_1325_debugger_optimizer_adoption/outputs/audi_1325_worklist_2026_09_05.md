# Debugger + optimizer — everything left, 2026-09-05

Supersedes `audi_1325_worklist_2026_09_04.md`. Every item below came out of a verified workflow this
session; nothing here is a guess.

## Done today

- PR #1289 merged, `deploy-2026-09-05T19-59-00` HEALTHY. First image carrying #1287.
- PR #1286 confirmed live AND executing: the 09:18 UTC sweep printed "No measured savings to report"
  with the 11-column table and a 90% CI. The old "115 hours / $32" is gone.
- Mode report `e81786de8403` publishes **$0**. Both corrected queries live and byte-verified, a new
  "Fixes not yet measurable" query added (`baf7ca81c920`), schedule moved 06:00 -> 10:00 UTC.
- Deploy date settled: the #1270-#1281 config went live **2026-09-03 20:21 UTC**, so AUDI-1328 scores
  on the **09-07** run, not 09-08.
- The "6 regressions" adjudicated: 5 artifacts, 1 real (`ipdsc_ds_49`).
- PR #1290 open (AUDI-1351), gauntlet PASS, 7 checks green.

## 1. Blocked on a person

| # | What | Why it needs you |
|---|---|---|
| 1 | **Merge PR #1290** | Branch protection requires an approving review. All 7 checks green. |
| 2 | **Rebuild the `optimizer.optimization_ledger` external table** | Needs write access to `mntn-prj-prod-00:optimizer`. Command is written and unrun in `artifacts/audi_1325_optimization_ledger_external_def.json`. Until then `prev_exec_h` and `partial` are silently dropped from every Mode query. |
| 3 | **Decide `ipdsc_ds_49`** (IMP-127) | A prod DAG config change. Revert `maxPartitionBytes` 64 -> 128 MiB (or 96) on that DAG only. Pre-fix band 0.2138-0.2428 DCU-h/GiB, post-fix 0.3013 and 0.3643, so 3 clean days each side separate by >6 sigma. Verdict 2026-09-09. |
| 4 | **P4, the LLM layer** | Not work, a decision: who owns reusing the prod `OPENAI_API_KEY` already set and read by `include/dbx/kube_operators.py`. The `why()` precedence already reserves the slot. |

## 2. Waiting on a sweep

| When | What unblocks |
|---|---|
| **2026-09-06 17:00 UTC** | First digest rendered by #1287. A wrapper failure should name the downstream exception, the subsystem that raised it, and the last frame in the job's own file. Compare against `conversion_signal_backfill_workflow/submit_batch_dsid_21`. |
| **2026-09-07 09:00 UTC** | AUDI-1328 scoring run. **53 keys across 13 DAGs** quiet since the fix and resolving at ledger date 09-06. 7 keys / 5 DAGs re-fired on 09-04. Command: `cd tickets/audi_1328_recommendation_validation && python3 audi_1328_score_recommendations.py --out outputs` |
| After item 2 + one sweep | Add `AND NOT COALESCE(partial, FALSE)` to the daily CTE in both Mode queries. Earlier it fails with "Unrecognized name: partial". |

## 3. Buildable now, no gate

**3a. Mode layout, four edits.** The page currently contradicts itself: $0 cards beside copy asserting
savings exist.
- Offset ~9489: "No measured savings outside Spark yet; BigQuery and Databricks rows start with the
  next daily sweep." Renders beside a $0 Spark headline and asserts Spark savings exist. **False.**
- Offset ~3263, hero: claims dollars use "the blended rate from actual Dataproc spend". The SQL uses a
  frozen `0.278` literal. The corrected query now returns `usd_per_exec_h`, `usd_rate_basis` and
  `usd_rate_measured_on`; read those instead.
- Offset ~3561: card subtitle "accrued since each fix merged" is no longer an accrual.
- Add a section rendering `ds('Fixes not yet measurable')`. **15 rows explaining why the number is
  zero are currently invisible on the page.** Also recheck "DAGs fixed so far", which reads 15 beside $0.

**3b. IMP-130, the `.inprogress` blind spot.** `crawl.crawl()` short-circuits any GCS path ending
`.inprogress`. `vertical_size_monitor`'s post-fix runs exist only as `.zstd.inprogress`, so its 3 keys
will resolve on 09-07 **from non-observation rather than from the fix working**. This is a silent
false-positive generator sitting directly in the path of the 09-07 scoring run, so it is the highest
priority item in this section. Either parse `.inprogress` logs or mark the key unobservable instead of
quiet.

**3c. IMP-128, the `exec_h` invariant.** `ledger.py:280` states "every entry's `exec_h` is its dag's
total for the sweep-day" but `append()` replaces only rows the current sweep re-emits, so 35 of ~470
DAG-day cells hold 2-3 values from different sweep generations. Either enforce it on write or document
that every per-DAG-day read is a MAX pick.

**3d. IMP-129, `mark_applied` clones the prior `exec_h`.** Any consumer that does not exclude
`state='applied'` rows reads the before-value as the after-value. Three of the six published
"regressions" were exactly this. Consider writing NULL.

**3e. Close AUDI-1329.** Still `in_progress`; the coverage answer is written.

**3f. P5 / IMP-107, the setup path.** Ryan Kleck asked for on-call AI/MCP tooling unprompted, which is
external demand for the thing ranked last. Re-rank it.

## 4. Known open, not scheduled

- `sweep.py:306` calls `savings()` with no `try`/`except`. Deliberate: a blanket except would mask the
  next real bug. Any future unhandled exception there still fails the sweep task.
- The `$0.04` per slot-hour constant in two Mode queries is sourced nowhere in either repo.
- `billing.py:23` pins `DCU_PER_EXEC_H = 5.44`, "the conservative end of the 5.4-9.9 range". At the top
  of that range every dollar figure is ~82% higher and nothing re-measures it.
- `fangorn_score_monitor` can never clear the gate: 3 sweep-days after its fix, 1 before, and no
  before-day can now appear.
- The AUDI-1351 branch is named `audi-1330-savings-residual-defects`. AUDI-1330 belongs to an unrelated
  triage Bug. Resolve the branch with `gh pr view <N> --json headRefName`, never by the number.
