Questions for Zach — Stage 3 VV IP Lineage Audit
Updated 2026-03-03 (pre-meeting version)

The audit is done. Analysis is validated (3.25M VVs, 15 advertisers,
BQ matches GP within 0.12pp). Production table query is ready to run.
These are the only open items before we can deploy.


================================================================
OPEN — Need your input before we can deploy
================================================================


1. Schema sign-off
------------------

Does the production table schema look right?

    ad_served_id, advertiser_id, campaign_id, cp_time
    bid_ip, vast_playback_ip, redirect_ip, visit_ip, impression_ip
    bid_eq_vast, vast_eq_redirect, redirect_eq_visit, mutated_at_redirect
    cp_is_new, vv_is_new, ntb_agree
    is_cross_device, el_matched, vv_matched
    trace_date (partition key), trace_run_timestamp

Full schema with column descriptions is in zach_review.md Section 4.

Anything to add or remove?


2. Where does the table live?
-----------------------------

I used "audit.stage3_vv_ip_lineage" as a placeholder.
No "audit" dataset currently exists in dw-main-silver or dw-main-bronze.

What project and dataset should this table live in?


3. Scheduling / pipeline ownership
------------------------------------

The INSERT query is ready (audit_trace_queries.sql, section A4b).
Pattern: DELETE + INSERT on a date range, idempotent, safe to re-run.
For daily loads: set trace_start = trace_end = target date.
Requires 30-day event_log lookback (e.g., for Feb 10, scan Jan 11-Feb 10).

What's the right mechanism — dbt model? SQLMesh? Airflow DAG?
And who owns the pipeline build-out?


4. Backfill range
-----------------

We've validated Feb 4-10 on BQ Silver. How far back should we backfill?

Note: each day of backfill requires a 30-day event_log scan,
so cost scales with the backfill range. Worth scoping before running.


5. Alerting
-----------

You mentioned creating alerts when lineage can't be resolved
(el_matched = false for a VV that should be CTV).

What system should those go to? Is there an existing alerting
framework, or does this need to be built?


================================================================
FOLLOW-UP — Needs Sharad, not you directly
================================================================


6. first_touch_ad_served_id for Stage 1 CTV VVs
------------------------------------------------

You said: "confirm with Sharad, but I do not believe they do this
lookup for stage 1 CTV VV."

This would explain the 40% NULL rate we see on first_touch_ad_served_id.
Not blocking the audit — we use last-touch as the primary trace.
But worth confirming for completeness.

Can you make the intro to Sharad, or should I reach out directly?


================================================================
RESOLVED — no action needed
================================================================

All analysis questions from the previous version of this doc are closed:

- Q1: first_touch batch backfill — ANSWERED: real-time log, no batch.
      NULLs are permanent, populated at write time.

- Q2: CTV-only vs all VVs — ANSWERED: "never assume only CTV. always
      assume all." Production table traces all VV types. Non-CTV VVs
      use impression_ip for bid IP (2-checkpoint trace).

- Q4: Last-touch vs full impression chain — ANSWERED implicitly by the
      above. One row per VV, last-touch. Full chain available via Q9/Q10
      queries if DDM ever needs it.

- Q5: Multiple clickpass rows per ad_served_id — RESOLVED: known edge
      case (0.038%), monitored. Production query deduplicates correctly.

- Q6: /32 CIDR suffix on event_log.ip — RESOLVED: GP inet artifact.
      Zero /32 in BQ silver. Defensive SPLIT() in production query.

- Dedup bug in A4b — FIXED: QUALIFY ROW_NUMBER() dedup already in A4b
      on both cp_dedup and v_dedup.

- ui_visits.click column — CONFIRMED: BOOLEAN, verified via bq show --schema.
