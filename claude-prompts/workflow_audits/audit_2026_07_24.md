# Workflow Audit — 2026-07-24

**Headline:** The request log is capturing tool/system tokens, not prompts — request-mining (one of the five learning loops) is blind. Fix the extractor first.

**Environment:** local · **Cards:** 90 (0 violations, 84 warns) · **Coverage:** 0 skeleton / 206 enriched / 57 verified / 0 stale · **Undocumented queue:** 10 · **KB health:** clean

_Generated by the first `/workflow-audit` run. Propose-only — nothing below has been executed. Approve items and they'll be run in batches._

## Tier 1 — Safe (mechanical, reversible)
- [ ] Remove 4 empty scaffold dirs (disk-only; recreated on demand) — `rmdir tickets/audi_1083_mm_classifying_view/meetings tickets/audi_1141_mm_vs_3p_by_vertical/meetings tickets/audi_1091_augmentor_full_source/artifacts tickets/audi_1091_augmentor_full_source/meetings` — _pure tree noise from the ticket template._

## Tier 2 — Judgment (your call)
- [ ] Set a standing policy on committed analysis CSVs so the audit stops re-flagging them. ~14 tracked `.csv` under `ti_896/outputs`, `ti_921/outputs` trip the "data-typed file tracked in git" check every run. They're legitimate analytical records — propose blessing `tickets/**/outputs/*.csv` as a carve-out in `folder_definitions.md` (or gitignore future outputs). — _one decision kills a recurring false-positive._
- [ ] Framing adoption is opt-in; 84 legacy cards warn. Don't blanket-frame. Propose `/frame` only on cards actively worked this quarter (e.g. `audi_1083`, `audi_1089` children if reopened). — _framing is a gate for new work, not a retro chore._

## Tier 3 — Standards (reconcile a rule)
- [ ] **Fix the request-log extractor (the headline).** `UserPromptSubmit` is logging system-reminder / tool / scratchpad tokens (`tmp`, `private`, `notification`, `task`, `output`, `tool`, `don`, `malachi`) instead of the user's actual verbs/nouns — so `request_digest.py` finds nothing recurring and the "propose a /skill" loop can't fire. Propose: tighten the extractor to strip the injected system/tool boilerplate and a stopword list before logging keywords. — _unblocks one of the five learning loops; highest leverage this week._
- [ ] Refresh the stale line in `claude-prompts/self_improvement_engine_plan.md` (§ intro): it still says the cadence trigger is *"the Pi-5 cron that already runs the Slack bot at midnight — invoking headless Claude."* The Slack bot was **decommissioned 2026-06-10** (MNTN bars API keys in local env) and the System-retro loop now runs as a native `/schedule` **cloud routine**. Propose updating that sentence. — _the plan that designed this loop is out of date with the shipped decision._
- [ ] 10 tables in `knowledge/bq/_UNDOCUMENTED.queue` → propose a `cataloger` pass on them to clear doc debt. — _skeleton→enriched is the standing enrichment queue._

## ✓ Clean this pass
- KB health: 0 stale docs, 0 orphans, 0 duplicate titles, `/capture` 1d ago, retrieval-eval 0d ago.
- Ticket lint: 90 cards, **0 violations** (all 84 warns are opt-in legacy framing).
- Coverage: 0 skeleton docs, 0 stale schemas.
- Standards drift: all skills referenced in CLAUDE.md (including `/workflow-audit`, added this commit).

## Skipped / needs attention
- Perf-drift offenders slice was empty this pass (recent log has no standout expensive/repeated queries) — non-blocking; re-check next week.
