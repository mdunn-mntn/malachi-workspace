# Workflow Audit — 2026-07-24

**Headline:** System is clean post-fixes; the only substantive signal is perf — a ~495 TB lift-query pattern (TI-837) and a ~95 TB tpa "sample" full-scan (TI-790) belong in the table docs.

**Environment:** local · **Cards:** 90 (0 violations, 84 warns) · **Coverage:** 0 skeleton / 206 enriched / 57 verified / 0 stale · **Undocumented queue:** 11 · **KB health:** clean · **Git:** 0 uncommitted

_Generated by the `/workflow-audit` skill (validation run). Propose-only — nothing below is executed. Approve items and they'll be run in batches. Supersedes the hand-authored placeholder from earlier today; the extractor-fix and pruning items it listed are now resolved (see ✓ Clean)._

## Tier 1 — Safe (mechanical, reversible)
- [ ] Remove 4 empty scaffold dirs — `rmdir tickets/audi_1083_mm_classifying_view/meetings tickets/audi_1141_mm_vs_3p_by_vertical/meetings tickets/audi_1091_augmentor_full_source/artifacts tickets/audi_1091_augmentor_full_source/meetings` — _template remnants, pure tree noise; recreated on demand._

## Tier 2 — Judgment (your call)
- [ ] Set a committed-CSV policy so the audit stops re-flagging analysis outputs. ~14 tracked `.csv` under `ti_896/outputs` + `ti_921/outputs` trip §1 every run. They're legitimate analytical records — bless `tickets/**/outputs/*.csv` as a carve-out in `folder_definitions.md` (or gitignore future outputs). — _one decision kills a permanent false-positive._
- [ ] Framing: 84 legacy `no framing_state` warns (0 violations). Don't blanket-frame — run `/frame` only on cards you reopen for active work. — _framing is a gate for new work, not a retro chore._

## Tier 3 — Standards / docs (reconcile a rule or capture a cost lesson)
- [ ] **Document the TI-837 lift-query cost pattern** (the highest-value doc update). Top offender billed **494,776 GB (~483 TiB), 8.23M slot-s**, and the same 8-table lift shape recurs **4×** at 100 TB+ each — the join of `raw.competing_vv` × `raw.clickpass_log` × `external.household_scoring__prospecting_intent__v1` × `raw.augmentor_log` × history physicals. Add the shape + a cost-guard note (date-bound, pre-aggregate) to those table docs. — _one recurring pattern dominates all-time spend; a doc note prevents the next 400 TB run._
- [ ] **Verify/annotate the TI-790 tpa "sample."** `raw.tpa_membership_update_log` billed **97,608 GB (~95 TiB) at 0.3 slot-s** for a query labeled "sample" — signature of an unfiltered full scan (or a mislogged dry-run). Add a partition/date-filter note to the `tpa_membership_update_log` doc; confirm the record is real. — _a "sample" should never scan 95 TB._
- [ ] Undocumented-table queue = **11** → a `cataloger` pass on the top offenders to clear doc debt. — _skeleton→enriched is the standing enrichment queue; grew +1 this week._
- [ ] _(light)_ This multi-part workflow-audit infrastructure has a self-review entry but **no Jira ticket**. Consider an AUDI ticket for visibility/story-points, or consciously accept it as untracked internal tooling. — _significant work normally gets a card; this was built ticketless._

## ✓ Clean this pass
- **Request-log extractor — validated fixed.** §6 top nouns are real work terms (`capture`, `ticket`, `xlsx`, `sheet`, `data`, `mntn`) — zero `task/notification/tmp/private` noise. Request-mining loop is un-blinded. (Residual `sure/make/see` are pre-fix records; the STOP additions self-heal as new prompts log.)
- **Signal-file pruning (§9) — live.** 1/8 files, under the keep-8 cap; ready `git rm` will appear automatically once the backlog exceeds 8.
- KB health: 0 stale / 0 orphan / 0 dup-title; `/capture` 1d ago; retrieval-eval 0d ago.
- Ticket lint: 90 cards, **0 violations**.
- Git hygiene: 0 uncommitted. Standards drift: all skills referenced in CLAUDE.md. Coverage: 0 skeleton, 0 stale schemas.

## Skipped / needs attention
- None skipped — ran locally, so §6 request-mining is included. §5 "repeated query shapes" (materialization candidates) slice is empty — no dedup/cache candidate surfaced this pass.
