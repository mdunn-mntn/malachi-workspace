# Workflow Audit — 2026-07-29

**Headline:** Reconcile `.claude/CLAUDE.md` with reality — it omits the new porting packager and still calls the decommissioned Slack bot "running."

**Environment:** local (request log present) · **Cards:** 103 (0 violations, 84 legacy warns) · **Coverage:** skeleton 0 · enriched 206 · verified 57 · stale 0 · **Kit:** verify.sh all hard checks ✓ · **Memory:** 155 files, MEMORY.md at cap (1.5k/1.5k)

This is the reasoning pass over the 2026-07-27 Pi signals + today's fresh local run (the 07-27 signals had no report — now processed). Propose-only: nothing below was executed.

## Tier 1 — Safe (mechanical, reversible)
- [ ] **Archive terminal `project_*` memories that are stale (>90d) and finished** — flip `lifecycle: archived` (reversible; keeps them grep-reachable) via `/capture`, which holds memory authority. Review-then-flip candidates: `project_review_cycle` (133d), `project_bidstream_initiative` (121d), `project_ti810_ryan_answers` (118d), `project_buk_loom_request` (117d), `project_rubric_rollout` (114d). — _each is a finished initiative aging in `active`; archiving cleans the lifecycle view. Confirm each is truly done before flipping (some `project_*` like `incrementality`/`fangorn` are still live)._
- [ ] **(Optional) Remove 19 empty scaffold dirs** under `tickets/audi_1049…`, `audi_1176…`, `audi_1141…`, `audi_1173…`, `audi_1091…` — `find tickets -type d -empty -delete` — _low value: `new_ticket.sh` recreates them on demand, so this is cosmetic. Skip unless you want a clean tree._

## Tier 2 — Judgment (your call)
- [ ] **Re-verify the 15 stale-active `feedback_*`/`reference_*` memories (>90d)** — open each, confirm still true, bump `last_verified` (or correct) via `/capture`. Highest-value to confirm: `feedback_source_table_ips` (138d), `feedback_mntn_only` (119d), `feedback_doc_style` (119d), `reference_eng_rubric` (114d), `feedback_paulo_direction` (133d). — _these encode operating rules/reference facts; if any drifted (e.g. review direction), a stale line silently misguides future sessions._
- [ ] **Merge the highest-overlap memory clusters via `/capture`** — top merge candidates: `fangorn` (5 files), `repo` (5), `scoring` (4), `bidder` (4). — _18 clusters share stem tokens; consolidating the biggest into one canonical file each reduces the near-duplicate surface. `/capture` holds the sole merge/delete authority — never auto-merge._
- [ ] **Cataloger pass on the 17 undocumented tables + push high-traffic `enriched → verified`** — start from `knowledge/bq/_UNDOCUMENTED.queue` and the `_COVERAGE.md` worst-first queue. — _only 57/263 tables are human-verified (~22%); the perf offenders (`cost_impression_log`, `all_facts`, `integrationprod.*`) are queried constantly and deserve verified docs._

## Tier 3 — Standards (reconcile a rule)
- [ ] **Add the porting packager to `.claude/CLAUDE.md`** so a future session can discover it. Proposed Key-Paths row:
  `| .claude/scripts/package_kit.sh | Port the kit to a fresh machine/job — emits a sanitized, domain-blind, generic-seeded bundle (+ bootstrap.sh + PORTING.md). Two acceptance gates (secrets + domain-blind). See memory reference_workflow_kit_porting. |`
  and one line in the "Self-Documenting System" section noting `package_kit.sh` builds a portable, cross-job-safe copy. — _a significant new capability shipped this week but is invisible from CLAUDE.md; only the memory + ai_workflow_kit README point to it._
- [ ] **Retire the stale Slack-bot description in `.claude/CLAUDE.md`** (contradicts reality — the bot was deleted 2026-06-10 by security policy; see `reference_pi5_server` + `slack_bot/RECOVERY.md`). Two edits:
  - Workspace Structure: `slack_bot/ ← Slack knowledge extraction bot (runs on Pi 5 at midnight PST)` → `slack_bot/ ← Slack knowledge bot — DECOMMISSIONED 2026-06-10 (security policy; see slack_bot/RECOVERY.md)`
  - Key Paths `slack_bot/` row: drop "Runs on Pi 5 at midnight PST. Add bot to channels via /invite" → "Decommissioned; migrate to a sanctioned platform. See slack_bot/RECOVERY.md." — _CLAUDE.md currently tells every session the bot is live; it isn't._

## ✓ Clean this pass
- **§2 Framing gate:** 0 violations across 103 cards. 84 legacy warns are opt-in (frame on next touch, no blanket action).
- **§7 Git hygiene:** 0 uncommitted/untracked.
- **§9 Signal backlog:** 2/8 — under cap, no pruning.
- **§11 Kit compliance:** verify.sh all hard checks pass (linters + index freshness + hooks 12/12). The 6 high-severity structure findings are blessed root carve-outs (`.githooks`, `lib`, `on-call`, etc.) — advisory.
- **§5 Perf:** top offenders are known/documented historical runs (TI-837 ~483 TiB lift, TI-790 tpa samples); the few 2× repeat shapes (~180–270 GB CPIV/CPIA pulls) are marginal cache candidates, not urgent.
- **§6 Requests:** 370 logged (7d); nothing recurs ≥3× → no `/skill` proposal this week.
- **§3 KB:** 0 stale docs, 0 dup-title, 0 orphans; last `/capture` 0d ago.

## Watch (not yet actionable)
- **MEMORY.md is AT the hot-tier cap (1.5k/1.5k).** Not over, so no trim needed — but the next always-on line pushes it over (Tier 3 trim then). Keep new task-specific facts out of `MEMORY.md` (grep-on-demand only).

## Skipped / needs local run
- None — this ran locally, so §6 request-mining is real.
