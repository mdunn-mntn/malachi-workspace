---
doc_type: backlog
title: Improvements Backlog
summary: "Single lightweight tracker for improvement ideas / durable fixes / tech-debt we want to remember but not put on the Jira board yet. On-call durable-fixes feed it; so can anything. Promote a row to Jira only when it's actually prioritized."
last_verified: 2026-08-15
keywords: [improvements backlog, tech debt, durable fix, follow-up, backlog, improvement ideas, deferred work, IMP-001, IMP-002, IMP-003, IMP-004, IMP-005, IMP-006, IMP-007, IMP-008, IMP-009, soft_fail sensor, dataproc quota, champion challenger staggering, tpa_mntn_id_export ttl, dataproc serverless ttl, maxExecutors, shuffle fetch, dataproc subnet networking, external task sensor, openai batch alerting, keyword_ddp_reporting, would-be ticket, not on jira, IMP-010, IMP-011, IMP-012, IMP-013, IMP-014, IMP-015, IMP-016, IMP-017, openai file storage quota, 2.5TB file quota, batch_submit, mntn_match_incrementals_submit, client.files.create quota, openai file hygiene, batch_fetcher, cost_impression_log campaign resolution, location_data metro_id, dataproc zonal stockout, multi-zone dataproc, fangorn inference fallback zone, us-central1-a stockout, product_categorization__max_dt, dbt freshness test backfill, current_date freshness test, smtp errno 99, email alerting broken, airflow.utils.email failure callback, IMP-018, write_targeted_signal_ds_19, DbxDbtOperator pod eviction, KubernetesPodOperator safe-to-evict, node preemption dbt python model, pod not found istio, deferrable pod operator, IMP-019, IMP-020, ruff enforcement, clean code gate, pyproject ruff, ruff phase 2, ruff phase 3, type hints lib, ANN mypy lib, IMP-025, wait_ds17_src freshness, existence sensor date validation, misdated backfill, mandatory partner sensor, INC-010, IMP-026, wait_fpa skip as failure, ExternalTaskSensor skipped_states, hashed_email_ds_26_signals, dsid26_predactiv_processing, predactiv DS26, source_available short circuit, cross-DAG contract, INC-011, IMP-105, test_url_pattern_pipeline REPO_ROOT, parents 2 path bug, IMP-107, IMP-108, IMP-109, IMP-110, company-wide adoption, debugger onboarding, optimizer onboarding, setup prerequisites, reply regression corpus, LLM remedy layer, key-free constraint, drive findings to zero, zero open findings campaign]
tags: [backlog, improvements, tech-debt]
---

# Improvements Backlog

One place for **improvements we've identified but don't want to open a Jira ticket for yet.** Keeps the
board uncluttered while making sure a good idea (a durable fix, a tooling gap, a cleanup) isn't lost.

## How to use
- **Add a row** whenever we spot an improvement worth remembering — an on-call durable-fix, a workflow gap,
  a code cleanup, a "we should really automate X." One line, then move on. Don't let it block real work.
- **Trigger for on-call:** the runbook's INC decision trees say "durable fix → spawn a ticket." Instead of
  opening Jira immediately, **log it here first**; promote only when prioritized.
- **Promote to Jira** only when a row is actually going to be worked. Set `Status: promoted:TI-XXX` and keep
  the row (don't delete — the history is useful). Then it lives on the board, not here.
- **Never delete** a `done` / `wontfix` row — flip its status. The log is the point.
- **Picking the next ID:** take the max across the whole file *immediately before writing*, not from an earlier read. Concurrent sessions share this worktree, and six IDs (025, 045-049) were issued twice that way and had to be renumbered on 2026-08-20. Check with `grep -o '^| IMP-[0-9]*' improvements_backlog.md | sort | uniq -d` — it must print nothing.

**Status values:** `idea` (captured, unranked) · `proposed` (recommended, awaiting a yes) ·
`promoted:TI-XXX` (now a Jira ticket) · `done` · `wontfix: <reason>`.
**Effort:** S (≤½ day / one-liner) · M (1–3 days) · L (>3 days, likely needs its own ticket).

## Backlog

| ID | Added | Area | Improvement | Trigger / why | Effort | Owner | Status | Ref |
|---|---|---|---|---|---|---|---|---|
| IMP-097 | 2026-08-31 | spark_optimizer / digest | Resolve an owner per digest row (every live row reads "unowned"): map dag_id to owning team or Slack handle (bundle owner tag or a small override map) and render it in parent rows and replies. | Malachi 2026-08-31 review of the live digest; deferred while relay verification and the open PR test run. | S | Malachi | idea | AUDI-1194 |
| IMP-001 | 2026-07-28 | airflow-ti / `ipdsc_monitor` | Set `soft_fail=_partner.optional` on the registry-driven optional-partner preconditions (and `mode="reschedule"`), so optional partners that don't drop (Bombora/DS51) SKIP instead of paging an 18h hard-fail. Mirrors `wait_{name}_src` in `tpa_ipdsc_export`. | INC-001: benign optional-partner skips page every time Bombora misses a drop; Jordan (Staff SWE) raised remove-vs-keep the sensor. Keeps QA coverage on drop days while killing the false page. | S | TPA_EXPORT (Jordan / Sean) | idea | on-call INC-001 |
| IMP-002 | 2026-07-28 | Fangorn / Dataproc | Stagger overlapping inference runs (or raise the Dataproc quota / add a concurrency guard) so a Fangorn-like inference pipeline and any concurrent Dataproc job don't collide at the ~94% Dataproc cap. | INC-002: a concurrent Dataproc job (even QA / a manual challenger) whose cluster overlaps the champion's create starves `create-dataproc-cluster` → code 9. Currently hand-re-triggered each time; a standing collision risk. (Note: the champion `inference_pipeline` and the DAG's `challenger_inference_pipeline` are SEQUENTIAL — inference is upstream of challenger — so they are NOT each other's contenders; INC-008 was a zonal stockout, not this.) | M | Fangorn/ML + infra (Brian) | idea | on-call INC-002 |
| IMP-015 | 2026-07-30 | Fangorn / Dataproc | Give the 290-worker Fangorn inference cluster quota+capacity headroom: (1) raise us-central1 N2_CPUS + DISKS_TOTAL_GB quota above the ~4,672/~145,500 request; (2) auto-delete a failed cluster-create immediately so its VMs don't quota-block the retry; (3) multi-zone/fallback-zone or secondary region; and/or (4) smaller cluster/machine family. | INC-008: the cluster needs ~4,672 N2_CPUS = ~93% of the 5,000 quota, so it has no headroom. It failed 3 ways in one afternoon — zonal `code 14` stockout (us-central1-a, -c), AND an `Insufficient N2_CPUS quota` (4672 vs 984 free) because attempt-1's FAILED cluster left ~250 workers (~4,016 N2) running until cleanup 45 min later (self-block). Distinct from IMP-002 (real concurrent-job contention). | M | Fangorn/ML + infra (Brian) | idea | on-call INC-008 |
| IMP-006 | 2026-07-29 | airflow-ti / tpa_ipdsc_export — TPA_EXPORT | Add producer-side wall-clock alerting on `tpa_ipdsc_export` / `run_geo` (repo pattern `dag_run_duration_watchdog`) so a 3h+ slip pages TPA_EXPORT proactively, instead of being discovered by a downstream consumer's sensor. Optionally identify + speed up the recurring long-pole `ipdsc_ds_*` builder (pull task-instance durations from the Astronomer metadata DB — the open gap from Compass §1). | INC-004 / Compass RCA: `ipdsc_geo` has recurring multi-hour tail latency (historical +14h/+16h outliers), not a one-off. The PR #1160 consumer sensor stops the page but doesn't address the producer running late. | M | TPA_EXPORT | idea | on-call INC-004; Compass RCA §5 |
| IMP-005 | 2026-07-29 | airflow-ti / audience_intent — Dataproc | Investigate the `fangorn_score_monitor` batch runtime (~66 min on 2026-07-28) and either raise its `ttl` (5400s) or reduce work (the 20K-file `prospecting_intent` read + ~5.2GB `ipdsc_geo` join dominate). It runs at ~73% of the 90-min TTL, so a heavier-data day risks a hard TTL failure. | INC-004 re-run: ~66 min vs 90-min TTL. Compass couldn't verify the trend from audit logs (Dataproc serverless emits no terminal-state event) — pull the duration trend from the job's own `SparkJobMonitor` output. Live risk: sibling `fangorn_prospecting_scoring` needed an emergency 6h TTL bump on 07-24 (PR #1147) for the same class of problem. | S | targeting (Ryan) | idea | on-call INC-004; Compass RCA §6 |
| IMP-004 | 2026-07-29 | airflow-ti / audience_intent | Add a `GCSObjectExistenceSensor` on `gs://mntn-data-archive-prod/ipdsc_geo/dt={{ds}}/_SUCCESS` before `fangorn_score_monitor` (mirror INC-001's `precondition_*` pattern), or widen its retry window, so it stops racing the `ipdsc_geo` producer. | INC-004: monitor reads `ipdsc_geo/dt=<run_date>`; producer lands on D+1 with ~3.5h-variable timing (04:56–08:17Z over 4 days); monitor's `retries=2×10min` gives only ~30-40min slack → pages on late-producer days with a `PATH_NOT_FOUND`. | S | targeting (Ryan) | done | on-call INC-004; PR SteelHouse/airflow-ti#1160 MERGED 2026-07-29 |
| IMP-003 | 2026-07-28 | airflow-ti / targeting-infra-ml — Fangorn Vertex | `TiVertexPipelineOperator` always injects `reference_date` into every submitted pipeline's `parameter_values`; a template whose date-param name drifts (drift pipeline's KFP source uses `run_date`) fails only at task-exec with a ValueError, not at DAG-parse. Standardize Fangorn pipeline param names on `reference_date`, or validate the operator↔template param contract at parse/compile time (fail fast / CI check). | INC-003: drift template declared `run_date` not `reference_date` → hard ValueError, PagerDuty, retries exhausted. The param mismatch is INVISIBLE at DAG-parse, so PR #1158 (airflow-ti) "fixed" it without touching the failing param and a re-run re-failed identically — the real fix is the targeting-infra-ml recompile. A contract check would have caught it in CI. | S | Fangorn/ML + infra (Brian) | idea | on-call INC-003 |
| IMP-007 | 2026-07-29 | `ti_resources_v2` / `tpa_mntn_id_export` model — targeting | **Fix the recomputation + spill in `tpa_mntn_id_export` (verified from the Spark event log, not a TTL problem).** (1) **Cache/persist `mntn_df`** (e.g. `.persist(DISK_ONLY)` or checkpoint) before the `_apply_audience_filter` loop + write, so the ~1.9TB `mntn_id` shuffle computes ONCE instead of the 7-9× recompute the event log shows. Biggest win. (2) **Raise `spark.sql.shuffle.partitions` 1000 → ~6000-8000** (~256MB/partition instead of ~1.9GB) to kill the 29TB memory + 14TB disk spill. (3) **Collapse the 14 sequential `crossJoin(F.broadcast(allow_one))` in `_apply_audience_filter`** into one broadcast intersect (one allowlist map, one select). (4) THEN a modest TTL bump as margin (precedent: sibling `fangorn_prospecting_scoring` 6h bump, PR #1147 — IMP-005). | INC-005 event-log deep-dive: batch cancelled at 3h because the final `.write.json()` recomputes a ~1.9TB shuffle 7-9× (uncached lineage) and `shuffle.partitions=1000` forces 29TB/14TB spill → tasks 70-97% shuffle-fetch-wait, 8-10% CPU, 0 executor loss, no OOM. NOT infra, NOT data volume. A TTL bump alone just lets the spill spiral run longer. All owner-side model changes. Draft PR [SteelHouse/airflow-ti#1161](https://github.com/SteelHouse/airflow-ti/pull/1161) implements all three. | M | Nivas Nalla (Ryan Kleck) — real fix/redevelop; PR by Malachi | proposed | on-call INC-005; PR airflow-ti#1161 |
| IMP-008 | 2026-07-29 | GCP infra / `dataproc-prod` subnet — infra | Route the **Dataproc Serverless networking anomaly** in the `mntn-prod-prj-snet-central1` / `dataproc-prod` subnet to whoever owns the subnet/SA config: run_geo workers `Failed to connect to master … Connection refused` + SIGNAL TERM after `_SUCCESS` (INC-004 Compass §). | INC-004 Compass RCA flagged it as a separate anomaly to route. (NOTE: INC-005's 33 FetchFailed were first suspected to be the same subnet issue but the event log reclassified INC-005 as a Spark perf bug, not infra — so this rests on INC-004 evidence alone unless the run_geo pattern recurs.) | M | infra / platform (via TPA_EXPORT) | idea | on-call INC-004 §Compass |
| IMP-009 | 2026-07-29 | airflow-ti / keyword_ddp_reporting + mntn_match_incrementals_fetch — targeting/ML | Alert on the **upstream OpenAI-batch failure directly** (`mntn_match_incrementals_fetch`) so a bad `product_categorization` cycle pages the pipeline owner immediately, instead of surfacing ~6h later as `keyword_ddp_reporting`'s downstream `ExternalTaskSensor` timeout. Better: switch `keyword_ddp_reporting` to asset/dataset-triggered scheduling on the `product_categorization` output instead of a time-boxed 6h sensor. | INC-006: `wait_for_product_categorization` timed out (6h) because the upstream OpenAI-batch pipeline didn't produce `product_categorization` for the cycle (GCS `dt=2026-07-27` absent). The downstream sensor is a lagging indicator of an upstream failure; the sensor itself is correctly configured. | M | targeting/ML (Victor Savitskiy / MNTN Matched pipeline) | idea | on-call INC-006 |
| IMP-011 | 2026-07-29 | `SteelHouse/sqlmesh` geo model (`location_data`) — BER | **Deploy the already-written AUDI-1072 fix.** PR SteelHouse/sqlmesh#1147 ("keep location_data.metro_id and hierarchy in sync" — a GENERAL type-6/7 metro_id COALESCE, already reflecting Sonali's review) is OPEN/DRAFT, never merged: `sqlmesh plan` tried to backfill/break downstream (needs **forward-only**) and Nivas couldn't run plan without gold access → **DEV-8264** ("Identity team read/write on **dw-main-gold**", In Progress, assignee Brian Teller). Handoff 2026-07-29: **Nate Gardner (Measurement)** owns resurrecting/shipping #1147 + a parallel network_locations fix; DEV-8264 was filed under Identity so Measurement likely needs its own gold access. Blocker detail (Nivas): the plan takes >24h but PAM grants dw-main-gold in 8h windows, so access expired mid-plan and it never finished; DPLAT now looking at deploying. Action: **deploy #1147 forward-only** (sidesteps the multi-hour backfill + PAM-expiry trap), not just a persistent-access request. Add a build-time DQ assertion (fail if any `location_type_id=7` row's metro `location_id` is absent from its `hierarchy`) to stop recurrence. **Open caveat:** #1147 doesn't touch `network_locations`, which is CIL's non-template-55 geo source (Nivas/lizz) — confirm #1147 alone clears the audit. | AUDI-1072 recurrence (aud22, 07-28, 6 non-WC CGs). AUDI-1072 marked Done because DM believed they'd fixed the root cause on the audit-logic side (unapproved-audience leak past the exclusion filter, DM tightened validation Jul-21), so #1147 was left undeployed. TWO candidate root causes now: (a) that audit-logic leak, (b) this location_data mismatch (#1147); Harry's 07-28 trace points to (b). Reconcile which drove 07-28 before/alongside deploying. Verified 07-29 at geo_version 1783900800: the 2 original ZIPs (43221, 45814) clean but 5 other US ZIPs (01223, 17371, 66214, 92545, 95245) still mismatch → affected ZIPs rotate each rebuild while the fix stays undeployed. Not per-CGID. | S–M | BER (Nivas Nalla / Sonali Vengurlekar); DEV-8264 (Edris/Tim Harrison) | proposed | aud22; AUDI-1072; PR SteelHouse/sqlmesh#1147; DEV-8264 |
| IMP-010 | 2026-07-29 | `SteelHouse/shopper_graph` / `openai/openai_wrapper/batch_fetcher.py` — ML/data-eng | Guard `download_file` (L27-45) against a `"completed"` OpenAI batch with `output_file_id=None` (all rows errored): null-check before `client.files.content(...)`, add terminal-status guards (failed/expired/cancelled → log+skip), wrap the fetch in try/except. Also gate `fetch_results.py` `update_source_file_s3()` on an actual successful download (return a bool), not unconditionally. (Weak secondary: bump `batch_fetch` pod memory >1Gi only if OOM is ever confirmed — per-file ~42MB makes it unlikely.) | INC-006 verified (source + GCS, not just Compass): `download_file` crashes on `files.content(None)` for a completed+errored batch AND has no per-batch isolation, so one bad batch aborts the whole loop and the rest never download; `update_source_file_s3` marked `was_downloaded` unconditionally, stranding incomplete batches. GCS proof: dt=2026-07-27 stuck at 928/1101 results, `results_joined` + `product_categorization` absent, next cycle (dt=07-28) not started. Real crash + stranding bug; the fix lets the loop finish and leaves skipped batches eligible to retry. **MERGED + DEPLOYED 2026-07-30:** shipped via `deploy_openai_dockerhub_gcp.yml` (run 30571986734, `openai_batch_runner:gcp-prod` from commit c6c8eda) — NOT the middleware deploy (wrong image); pending first-run validation 2026-07-31 09:00Z. | S | ML/data-eng (Sean Yang / Ryan Kleck) | done | on-call INC-006; PR [shopper_graph#296](https://github.com/SteelHouse/shopper_graph/pull/296) MERGED+DEPLOYED 2026-07-30 |
| IMP-012 | 2026-07-29 | `SteelHouse/sqlmesh` `cost_impression_log` model (campaign-id resolution) — BER/data-platform | **Fix the CIL campaign-id resolution re-stamping real impressions to `campaign_id = -3` on reprocess.** PROVEN (INC-001): `cost_impression_log dt=2026-07-27` for adv 30506 campaigns 648318-648323 (Bombora CG 131563) = 0 under the real ids but **110,750 under `-3`** (unresolved sentinel); spend_log 110,792 ($904 billed, 100% rendered) + win_logs 110,862 confirm the impressions are real; time-travel shows 109,530 correctly attributed 47h ago → 0 now (a reprocess regressed resolved→`-3`). 07-28 partially hit too (102,456 as `-3`). Campaigns are new (created 07-24) → likely a dim-sync/ordering race in the campaign-resolution join. Add a build-time DQ assertion (fail/alert if `campaign_id=-3` share spikes for an advertiser-day, or reconcile CIL resolved-count vs spend_log per campaign×day). | INC-001: the mis-resolution zeroed `enriched_impressions` DS51 and sent a multi-round investigation down DS51/ipdsc/serving/data-loss dead-ends. Test advertiser so low business impact, but the same regression would silently mis-attribute (and under-report per-campaign) real advertisers. | M | BER / data-platform (CIL SQLMesh owner) | proposed | on-call INC-001 |
| IMP-013 | 2026-07-30 | `SteelHouse/shopper_graph` / `openai_batch_runner` (`batch_submitter.py` + cleanup) — ML/data-eng | **Add OpenAI file-storage hygiene + headroom so `batch_submit` stops hitting the 2.5TB project file quota.** (1) Purge old OpenAI input+result files every cycle regardless of batch outcome (not only after a successful download in `batch_fetcher`) — e.g. a scheduled `client.files.list()` sweep deleting already-processed / >N-day files. (2) Quota-headroom guard before `batch_submit`: sum `client.files` usage and alert (or self-purge) at ~2.0TB so it pages proactively instead of via a downstream sensor a day later. (3) Surface `batch_submit`'s failure as an explicit "OpenAI file-storage quota exceeded" alert, not a generic pod failure. | INC-007: `batch_submit` (logical 07-28) failed all 4 tries with OpenAI 400 "exceeded file storage quota (2.5TB)" at `client.files.create` → no `openai_batch_submissions/dt=07-28` → cascaded to keyword_ddp's `wait_for_product_categorization` a day later. Deterministic quota wall (retries can't clear). Quota self-cleared via old-file expiry but recurs without headroom management; likely aggravated by INC-006's leaked undeleted files (#296 slows the leak, doesn't purge the backlog or add headroom). **Ticket already exists — [AUDI-1042](https://mntn.atlassian.net/browse/AUDI-1042) "product_categorization: OpenAI storage quota issue"** (Victor, Backlog/P3 since 2026-06-18, unassigned): prioritize + assign it, don't open a new one. Owner meeting 2026-07-30 confirmed `batch_cleanup` (deletes old files daily) was outgrown by volume; **Victor departed** → under-owned. Immediate mitigation agreed: ask OpenAI for more free storage via the account owner. | M | ML/data-eng (Ryan Kleck; Victor departed) | in progress | on-call INC-007; **AUDI-1042 In Progress (P1, Malachi)**; PR [shopper_graph#297](https://github.com/SteelHouse/shopper_graph/pull/297) **MERGED+DEPLOYED 2026-07-30** (part 1 cleanup fix — per-file delete + pagination, run 30577185770; parts 2 headroom-guard + 3 explicit-quota-alert still open) |
| IMP-016 | 2026-07-30 | `SteelHouse/shopper_graph` `dbt/tests/mntn_matched/post_batch/product_categorization__max_dt.sql` — ML/data-eng | **Key the `product_categorization__max_dt` freshness test off the run's logical date, not wall-clock `current_date`.** The test asserts a partition exists for `date_sub(current_date, 2)`, but the pipeline writes `dt = yesterday`(-of-logical-date). On a normal on-time run these coincide; on any late or manual backfill they diverge and the test fails spuriously. Change the predicate to the run's `yesterday`/`run_date` env var (already injected into the pod) so a backdated re-run validates the partition it actually wrote. | INC-007 backfill: re-running `mntn_match_incrementals_fetch` logical 07-29 on 07-31 produced `dt=2026-07-28` correctly, but the test looked for `current_date-2 = 2026-07-29` → 1-row FAIL, failing an otherwise-clean backfill (all substantive checks passed, `dt=07-28/_SUCCESS` in GCS). Forces a manual mark-success on every backfill of this DAG. Confirmed from source 2026-07-31: `max_dt` asserts `where dt = date_sub(current_date, 2)`; the other 5 product_categorization tests key off `max(dt) where dt >= date_sub(current_date, 5)` (latest-partition), which is why they PASSED on the backfill while only `max_dt` false-failed. Observed on `batch_test.test_product_categorization` try-4 (2026-07-31 01:43Z, PASS=4 WARN=1 ERROR=1) AFTER dt=07-28 was already verified (_SUCCESS + parquet) on 07-30. Fix = swap the predicate to the pod's `yesterday` env var. | S | ML/data-eng (Ryan Kleck) | idea | on-call INC-007 backfill |
| IMP-014 | 2026-07-30 | `SteelHouse/airflow-ti` `gcp_mntn_global_data` DAG (DS14 builder) — targeting | **Add build-health alerting for the DS14 daily build.** The DAG runs `severity=0` with no Grafana/Prometheus job, and its PySpark reads each of the 3 sources (augmentor_log, bidder_auction_events, guid_log) inside a per-source `try/except` that only warns, so a day where augmentor and/or auction fail to land still "succeeds" on guid_log alone. Add a non-zero severity + alerts on (a) any source read raising/warning and (b) DS14 daily distinct-IP count dropping >X% vs trailing median. | Compass code read (2026-07-30, DS14 build verification): fail-soft `try/except` per source + `severity=0` + no dashboard = a multi-day partial-source outage silently shrinks the DS14 addressable pool (and, if the 8-day serving TTL is the live path, the biddable pool) with no signal. | S | targeting (Ryan Kleck — airflow-ti DS14 builder) | idea | Compass DS14 build verification 2026-07-30 |
| IMP-017 | 2026-07-31 | Astronomer / airflow-ti — failure-alert email | Fix or drop the Airflow failure-alert EMAIL callback. On any task failure `airflow.utils.email` attempts SMTP and dies with `OSError: [Errno 99] Cannot assign requested address` (worker pods can't reach the SMTP host), so failure emails never send and the SMTP traceback prints as the visible tail of the task log, masquerading as the task's own error. Point it at a reachable relay, or drop email in favor of the existing PagerDuty/Slack alert path. | INC-007 backfill: the Errno 99 SMTP traceback was the visible tail on `batch_test.test_product_categorization` (and earlier on `batch_cleanup_2`), red-herringing the real dbt-test failure. Cosmetic to the data, but it hides root cause on every failed task and means email alerting is silently broken for this deployment. | S | infra / platform (Astronomer) | idea | on-call INC-007 backfill |
| IMP-018 | 2026-07-31 | `targeted_signal` Databricks job (dbt-ml `generic_dbt_runner_ml`) + `DbxDbtOperator` orchestration — ML | **Stop `write_targeted_signal_ds_19` from paging + spawning colliding duplicate runs (RECONCILED root cause: the Databricks job SUCCEEDS + writes data; the failure is orchestration — the KPO pod 404s so Airflow loses a successful job, and a retry launches a duplicate that hard-fails on `TABLE_OR_VIEW_ALREADY_EXISTS`). Data is never at risk.** 4 levers, priority order: (1) **Orchestration** — make the pod survive the full ~2h (`safe-to-evict:false` / non-preemptible / longer deadline) OR make `DbxDbtOperator` **cancel its Databricks run on pod termination** so a dead pod neither orphans nor lets a retry spawn a colliding duplicate. (2) **Perf (cut the ~2h so the pod outlasts it)** — the model re-dedupes ALL `prod.mntn_matched.product_categorization` history each run (13.5B rows / 481 daily partitions / ~2 TiB shuffle / multi-TiB spill); run `ANALYZE TABLE … COMPUTE STATISTICS` (plan flags stats MISSING) + bound/materialize "latest categorization per composite_key" instead of scanning all 481 days. (3) **Idempotency** — the catalog CREATE should tolerate an existing table (CREATE OR REPLACE / handle concurrent create). (4) **Spot/RAM** — off pure spot (`first_on_demand`↑ / `ON_DEMAND_GCP`) + highmem node to cut the 4.9 GiB/task spill; secondary, reduces preemption that triggers the pod death. | INC-009: pod `write-targeted-signal-ds-19-822bmvra` 404'd (`pods … not found`); the Databricks job it launched SUCCEEDED anyway (verified: 1,200 parquet files at `signals/targeted_signal/data_source_id=19/dt=2026-07-30/`, matching the plan's 1,200-file/1.8B-row `InsertIntoHadoopFsRelationCommand`). Sean Yang manually marked ds_19 success + let ds_13 run; flagged we need a long-term fix if it recurs. A concurrent retry hit `TABLE_OR_VIEW_ALREADY_EXISTS` (SQLSTATE 42P07) = the duplicate-run collision. ~2h runtime (full-history dedup) is why the pod outlives its ~50-min window and dies. Feeds vendor payments. | M | ML — **we own the fix (Malachi)**; Sean Yang + Ryan Kleck advising | proposed | on-call INC-009 |
| IMP-019 | 2026-07-31 | Workspace kit — ruff Phase 2 (`pyproject.toml` durable select) | **Widen the durable-tier ruff select to add `N` (pep8-naming) + `C901` (mccabe, `max-complexity=12` warn-band).** Staged-scoped, so it only bites files you touch. Human decision: whether the branchy index-builders warrant a higher `max-complexity`. | Phase 2 of the clean-code enforcement plan. Phase 0+1 (adopt ruff, one-time reformat, wire the gate) shipped 2026-07-31. | S | Malachi (workspace kit) | done | shipped 2026-07-31 (commit f8d46d46); `clean_code_enforcement_plan.md` §8 |
| IMP-020 | 2026-07-31 | Workspace kit — ruff Phase 3 + mypy (`lib/` type/docstring contracts) | **Add `D` (pydocstyle `convention=google`) + `ANN201`→`ANN001` scoped to `lib/` only (per-file-ignore drops `.claude/scripts`), and stand up manual `mypy lib/` — advisory, NEVER in the commit gate** (pandas/openpyxl noise trains `--no-verify`). Closes the 3/121-typed gap on the reused library API; annotate `lib/mntn_xlsx.py` public API first. Biggest self-documentation win (typed signature = contract for humans + Codex). | Phase 3 of the plan. `lib/mntn_xlsx.py` already imports `from __future__ import annotations` → low-friction. | M | Malachi (workspace kit) | done | shipped 2026-07-31 (commit aa1ac666); lib/mntn_xlsx.py fully typed (179 findings, mypy clean); mypy stays advisory, never gated |
| IMP-021 | 2026-07-31 | On-call automation — Ryan Kleck's `data-ing-ai` repo (agentic Airflow/Spark auto-triage) | **Revive + extend Ryan Kleck's agentic on-call-triage repo** (offered handoff 2026-07-31): on an Airflow failure it reads the Spark UI / explain plan, finds the stuck stage, and emits a summary + candidate PR (or "can't fix, it's GCP"). Ryan had the bones (a `SparkJobMonitor` that logs the explain plan) but hit access walls (API tokens/MCP revoked per vault policy; Slack bot deleted) so it's not fully automated. Extends the workspace AI kit / `/oncall`. | INC-009 meeting: repeated manual Spark-UI spelunking is slow + Victor-dependent; both Ryan and Malachi want it automated. Constraint = programmatic Databricks/Airflow access is policy-gated (no long-lived tokens), so it lands as a human-in-the-loop assist, not a Slack-integrated bot. | L | Malachi (Ryan hands off the repo) | promoted:AUDI-1190 | on-call INC-009 meeting |
| IMP-025 | 2026-08-04 | AUDI-1191 — share the Spark debugger/optimizer with the team (when polished) | **Low-key per Ryan (2026-08-04): no big ticket now — finish + test, then share** ("hey guys I've got this cool thing"). Broader use = an MCP tool / API-key automation so anyone can run it without SSO (Ryan's "base camp" model; DevOps registers it). Also **Scala Spark support** for the identity team (event logs are engine-agnostic → parser likely already works; verify). Asterisk item, revisit when the tool is trusted in real use. | Ryan wants it shared once mature; keep it under the radar until then. | S | Malachi (flag Ryan) | idea | Ryan meeting 2026-08-04 |
| IMP-024 | 2026-08-04 | `Update Vertical Categorization` Spark job — chronic Stage-0 skew (up to 242x) | **The AUDI-1191 optimizer crawl surfaced this on real prod event logs (`gs://mntn-data-archive-prod/spark-events`).** Every run of the `Update Vertical Categorization` model skews on Stage 0 (10-242x max-vs-median task; one partition holds nearly all the data), and it shows GC pressure (memory-starved). Fix = salt the skewed group/join key or enable AQE skew join (`spark.sql.adaptive.skewJoin.enabled`); a plain repartition won't fix a value-skewed key. Owner = vertical-categorization model owner (Ryan Kleck (corrected from Sean Yang 2026-08-07; DAG is TGT/rkleck, schedule=None manual-only; 242x is duration skew from the pre-discriminator detector, re-profile next manual run before fixing) / DDP). | First real optimization target found autonomously by the tool; concrete wall-clock + cost win. | S | Malachi flag → model owner | in_review | AUDI-1191 prod crawl 2026-08-04 |
| IMP-023 | 2026-08-03 | AUDI-1191 — enable Spark optimization-data collection (both engines) | **Config-only prod changes so a completed job's Spark plan/metrics land where our key-free analyzer reads them** (spec: `tickets/audi_1191.../artifacts/audi_1191_optimization_data_enablement.md`). Probe proved the plan/metrics are unreachable today for a terminated run. **Dataproc:** set `spark.eventLog.enabled=true`+`.dir`+`.compress` in the batch `runtime_properties` (`spark_job_monitor.py:144` already warns it's off) → `.zstd` event log → `eventlog_profiler.py`; + attach a persistent Spark History Server (`spark_history_server_config` shape exists, `ipdsc_emr_cluster.py:68`) for historical retention. **Databricks:** cheapest = `EXPLAIN COST`/`df.explain(mode="cost")` in the dbt model → plan+optimizer-stats to stdout (already in `get-run-output`); durable = add `cluster_log_conf` to `new_cluster` (`operators.py:481`) or enforce via cluster policy `001D160AE4052091` (one edit, all job clusters). | Unblocks use-case #2 (optimization crawl). Historical pre-change runs stay uncollectable (going-forward only). **Dataproc half SHIPPED as PR [airflow-ti#1169](https://github.com/SteelHouse/airflow-ti/pull/1169)** (eventLog central + PHS removal + observe wiring; awaiting review). Databricks half still pending (mountain-devops GCS-write PR). | M | Malachi (AUDI-1191) + Ryan/owner + policy owner | pr-open | AUDI-1191 acquisition probe 2026-08-03; PR 2026-08-04 |
| IMP-022 | 2026-08-03 | AUDI-1191 `airflow_debugger` — Phase 3 (gated on the read-only RCA being trusted in real use) | **The deferred/gated automation phase of the debugger.** Three items, each blocked on an external gate: (1) **in-DAG auto-fire** — a shared `on_failure_callback` injected via `airflow-ti include/job_config/job_config.py` (`JobConfig.make_dag_args`) so every team DAG self-diagnoses without the `--watch` sensor; touches prod, feature-flag + Ryan's review, never push main. (2) **Sanctioned Slack thread-reply** — post the RCA as a reply on the alert; blocked by the 2026-06-10 no-bot policy (needs a sanctioned Slack app, not a token-holding bot). (3) ~~**Propose-only PR**~~ **DROPPED 2026-08-20 by user decision: the tool must never open a PR, push a branch, or commit code, in any form. Read-only GitHub access (resolving a traceback frame to a file link) is acceptable; writing is not.** Original scope was (claude-code-action: push branch + prefilled PR link, never auto-merge, path-restricted) + **adversarial-reviewer** subagent (fresh context, "assume it's wrong"). | Phases 0-2 + the `--watch --diagnose` loop shipped 2026-08-03 (validated on INC-005 + INC-009); both research tracks + AUDI-1190 say hold these highest-risk pieces until the read-only RCA is trusted. **Hold LIFTED 2026-08-25 for Slack delivery only** (AUDI-1221): per-failure `on_failure_callback` threading a diagnosis onto the alert in `#alerts-tpa-pipeline`. Auto-PR stays permanently dropped; fix depth is pointer-only. | M | Malachi (AUDI-1191) | idea (Phase 3 HELD 2026-08-20; auto-PR permanently dropped) | AUDI-1191; deferred per approved plan |
| IMP-057 | 2026-08-05 | airflow-ti / `tpa_ipdsc_export` — mandatory partner sensors | Make mandatory `wait_<ds>_src` sensors validate the DELIVERED data's date, not just object existence, so a stale/misdated backfill copy doesn't silently pass a retry. Options: assert the object names carry the requested `date=<D>` (ShareThis names files `…ip-<YYYYMMDD>-*`), or a manifest/`_SUCCESS` freshness gate. | INC-010: ShareThis (DS17) missed the Aug-3 drop; the unblock copied Aug-2 data into `date=20260803/`, and the existence-only `GCSObjectsWithPrefixExistenceSensor` passed try2/try3 on the wrong-dated copy → the pipeline ran on stale data undetected. Sean shipped a per-feed day-1 fallback (embraces prev-day on late source); this is the stricter cross-feed detection option. | S | TPA_EXPORT (Sean) | idea | on-call INC-010 |
| IMP-026 | 2026-08-05 | airflow-ti / `hashed_email_ds_26_signals` — `wait_fpa` skip-as-failure | Make the `wait_fpa` `ExternalTaskSensor` propagate an upstream SKIP instead of paging: set `skipped_states=[State.SKIPPED]` so a legitimate no-source-data skip makes the consumer SKIP (not FAIL), OR gate `hashed_email_ds_26_signals` with the same `source_available_dsid26` short-circuit the producer uses. Aligns the cross-DAG contract with the producer's intentional data-driven skip. Prod DAG change — owner-side, never hot-patch. | INC-011: Predactiv (DS26) missed one hourly file; producer `fpa_site_visit_batch_serverless` SUCCEEDED and correctly skipped `dsid26_predactiv_processing`, but `wait_fpa`'s `failed_states` counts a skipped external task as a failure → a benign partner-data gap pages prod. Recurs whenever DS26 (or any short-circuited source) drops an hour. MERGED (PR #1175, 2026-08-05, 2 DAGs: ds26 + guid_log/DS23; keyword_ddp excluded, its skip is a real break). Tracked AUDI-1195 (Spike). | S | Targeting/TI + Malachi (PR) | done | on-call INC-011 |
| IMP-027 | 2026-08-06 | airflow-ti / `materialize_mntn_select` Spark script — fragile full-prefix GCS listing | Harden input discovery in `spark/data_source/materialize_mntn_select.py`: expand `region={east,west}` into two literal paths for `get_paths` + the `.parquet()` read (no wildcard → leaf-dir listing only), and/or set `spark.hadoop.fs.gs.glob.flat.enable=false`. `fs.gs.http.read-timeout` bump is a band-aid. Small PR; same pattern check worth running on `materialize_mntn_first_party.py` (same `get_paths` helper). | INC-012: the GCS connector resolves the glob by flat-listing everything under `augmentor_log/region=` then filtering — O(entire prefix). Two prod tries died at ~19 min on `SocketTimeoutException` during that list; hh=19 output hole. Re-runs pass (latency variable) so it will page again. | S | TPA_EXPORT (Sean) + Malachi (PR #1176 + #1177) | done | on-call INC-012; v1 #1176 incomplete (basePath still statted the root); v2 #1177 MERGED 2026-08-06 |
| IMP-028 | 2026-08-06 | airflow_debugger / `dataproc_rca` — driveroutput fallback | When Cloud Logging returns no driver text, parse the staging `driveroutput.*` URI from the batch `state_message` and read it via gsutil (`check_hashes=never`); surface `application_id` + `event_log_uri` too. INC-012's real cause lived ONLY there; the analyzer suggested the path in its own output but never tried it (confirmed by the 12-agent step test: error_text/signature null on a live failed batch). Needs staging-bucket read (PAM interim / standing grant later); degrade gracefully on 403. SHIPPED 2026-08-06: `driveroutput_uri` + `_driveroutput_text` in `dataproc_rca.py`, fires only when Cloud Logging yields no error text; on the real INC-012 driver text the full chain returns `gcs_list_timeout` (fixture test); without PAM it notes the 403 + unblock step (verified live, both egress-flake and 403 paths). | S | Malachi (AUDI-1191) | done | 12-agent step test 2026-08-06; INC-012 |
| IMP-029 | 2026-08-06 | airflow_optimizer / eventlog v2 rolling dirs | Handle `eventlog_v2_*` rolling event-log DIRECTORIES (`events_1_<app>`, `events_2_...` parts): the crawler currently expects single `.zstd` files, so a rolled multi-part log is read partially or skipped. Archive bucket already holds both shapes (360 objects incl. eventlog_v2 dirs, seen in the O1 step test). Fetch all parts, parse in sequence. Also: synth extractor fed the LLM null signals on a minimal log (D8 gap, minor). | S | Malachi (AUDI-1194) | done | 12-agent step test 2026-08-06; FIXED 2026-08-07: eventlog.py reads all events_* parts in numeric order + test; validated on aud-int-int-map (4-part rolling dir, Ryan's ask) |
| IMP-031 | 2026-08-07 | airflow-ti — apply the INC-012 fix pattern to ALL remaining fragile `augmentor_log`/`bidder_auction_events` readers | Three scripts still hand the huge root prefix to the GCS connector (glob flat-list and/or basePath root stat): `spark/fpa/dsid30_augmentor_log_processing.py:29` (INC-013, failing now), `spark/auction_log_augmentor_process_gcs.py:141-162` (`region=*` + basePath, BOTH prefixes, daily dt-level), `spark/create_mntn_global_data_pyspark.py:62-90` (`region=????`+`hh=??` + basePath + recursiveFileLookup, BOTH prefixes, inside try/except so a timeout SILENTLY drops the day's data). Fix = #1176+#1177 pattern: literal region paths, drop basePath, existence-guard. PR [airflow-ti#1179](https://github.com/SteelHouse/airflow-ti/pull/1179) covers all three (2026-08-07). | INC-013: dsid30 failed-failed 3x on 2026-08-07 (holes hh=06/07/12 both outputs); retries masked it since 2026-05-07. The silent create_mntn_global_data case is the worst failure mode (no page, missing data). MERGED 2026-08-07 16:22Z + prod-verified (dsid30 15Z retry 6 min, hh=14 landed). Silent-degrade CONFIRMED: mntn_global_data dt=2026-08-06 green with zero augmentor rows; re-runs deferred by user. | S | TPA_EXPORT/Targeting (Sean) + Malachi (PR) | done | on-call INC-013 |
| IMP-032 | 2026-08-07 | airflow_debugger + airflow_optimizer — perf-class handoff | When the debugger's signature is perf-shaped (ttl_exceeded, driver_oom, fetch_failed) AND the batch has an event log (has_event_log/event_log_uri already on the evidence), auto-run the optimizer's eventlog profiler on that batch and fold the top findings (spill, skew, uncached recompute) into the troubleshooting pack. Escalation only for that class; never on every failure (most failure classes need no query detail, and logs are often absent/PAM-gated). | INC-005 is the proof case: the event-log deep dive explained WHY the TTL was hit. User design discussion 2026-08-07. | S | Malachi (AUDI-1194) | done | AUDI-1191/1194; SHIPPED 2026-08-07: perf_profile.py escalation bridge (gate=ttl_exceeded/driver_oom/fetch_failed + has_event_log), batch_uuid rolling-dir resolution, troubleshooting-pack section, 5 tests |
| IMP-030 | 2026-08-06 | airflow_debugger / `report` — standard incident write-up template | Extend `report.py` beyond the ≤500-char BLUF: a standard write-up section that always has (1) Problem — what broke and why, (2) Solution — what fixes it, (3) PR link auto-attached when one exists, (4) affected code areas as repo file links (the failing script, the DAG, the call site). The signature taxonomy already carries `programmatic_fix`; add a `fix_ref` field (PR URL + file list) populated at incident close so a repeat match renders the full structure automatically. User ask 2026-08-06. SHIPPED 2026-08-07: fix_pr/fix_files on INC-011/012/013, build_troubleshooting + code_links in report.py, --troubleshoot CLI; 6-agent adversarial review found 3 real defects (wrong-file collision link, framework-frame leak, unrelated-PR claim), all fixed with regression tests; proven live on the INC-013 log. | S | Malachi (AUDI-1191) | done | user request; INC-011/INC-012 write-up shape |
| IMP-033 | 2026-08-07 | airflow_optimizer / OSS plan-format detectors | analyze_plan's scan/stats regexes only match Databricks EXPLAIN COST text ('Scan parquet <catalog.table>', '== Optimizer Statistics =='); Dataproc Serverless physicalPlanDescription emits 'FileScan parquet [] ... InMemoryFileIndex(gs://...)' and no stats block, so the plan side yields 0 findings on the Dataproc fleet (confirmed 159/159 real plans). Write OSS-format patterns (FileScan sources, Exchange sizes from AQE final plans) so plan findings work on both engines. Tail-cap fix (2026-08-07) already preserves the physical plan section. **WIDENED 2026-08-20 against a live Databricks EXPLAIN COST:** only `missing_statistics` fires even on real Databricks output. The shuffle-size regex wants `(ShuffleQueryStage|Exchange) ... sizeInBytes=` on one line, which is the Spark UI's SQL-tab rendering; EXPLAIN COST attaches `Statistics(sizeInBytes=)` to LOGICAL operators and Photon renames the physical ones to `PhotonShuffleExchangeSink/Source`. So `broadcast_candidate`, `shuffle_partition_sizing` and `window_full_sort` are dead on BOTH engines, not just OSS. Fix targets the plan rendering, not the engine. **MEASURED 2026-08-26 on the full archive:** 295 of 300 sampled Dataproc runs DO carry SQL plan text (4,734,637 chars total) and `parse_plan_text` extracts **0** leaf scan nodes from all of it, so all four plan detectors reported nothing across 2,954 logs. OSS writes `Relation [cols...] parquet`; the regexes want `Scan parquet <table> ... Statistics(sizeInBytes=...)`. The unblock is now identified: `system.query.history` on Databricks carries the statement text `system.lakeflow.job_run_timeline` lacks, gated on one `USE SCHEMA` grant an account admin must run. **GRANT LANDED 2026-08-26** and the bridge reads: after `heavy_queries` was deduped to one statement per dbt node, 4 of 8 node's plans parse. The 4 that do not all reference `prod.ml.ddp_vertical_classification_api`, which is dropped and recreated ~20x/day, so a replayed EXPLAIN COST can never resolve it. The OSS-format regexes are still unwritten. | S-M | Malachi (AUDI-1194) | idea | AUDI-1194 hardening pass 2026-08-07, defect plan-pipeline-dead-3/optplan-1; widened 2026-08-20 by the live EXPLAIN COST validation; measured 2026-08-26 on the full-corpus validation |
| IMP-034 | 2026-08-09 | airflow-ti / `populate_data_source` ds17 — self-heal static mapping read | Snapshot the static sharethis categories mapping to a safe bucket on each successful read + fall back to the last-good copy when the source path is missing (self-healing against source deletion). | S | Targeting (Sean) | idea | on-call INC-014 |
| IMP-035 | 2026-08-09 | mntn-data-partners — static-file expiry watchdog | Weekly audit listing static files in `mntn-data-partners` vs the bucket's lifecycle rules; warn at age ~350 before the age-365 Delete rule kills them. No prod change. | S | Malachi | idea | on-call INC-014 |
| IMP-036 | 2026-08-10 | vertical_categorizations — quarterly blocklist/whitelist refresh | Re-run the AUDI-431 pipeline (candidates from missing_domains, bands from ddp_url_verticals scores, QC pass) each quarter; fully scripted in tickets/audi_431_blocklist_whitelist/artifacts/. Lists went 11 months stale last cycle. | S | Malachi | promoted -> AUDI-1202 (due 2026-11-11) | AUDI-431 |
| IMP-037 | 2026-08-10 | website_crawl_verticals — backfill seeded with whitelist adds + fix 76 wrong verticals | Next wcv crawl refresh: seed with AUDI-431 whitelist adds (vertical-less until crawled) and apply the 36 LIVE agreed-wrong corrections (facebook.com→B2B Sales & Marketing leads; 413M urls/7d). The other 40 wrong ones are blocklisted so ip_vertical_associations already skips them. Also reconcile the 365 domains in both lists. 26 corrections DEPLOYED to vertical_manual_overrides 2026-08-11 (apply on next wcv rebuild); 10 not-verticalizable blocklisted instead. | M | Ryan Kleck | partly done | AUDI-431 |
| IMP-038 | 2026-08-10 | bq_run.sh — stats fetch fails on external-table jobs (jq startswith error) | Jobs whose only inputs are external tables error in the post-run jq stats parse ('startswith() requires string inputs' on null referenced tables), so no perf-log record lands. AUDI-431's 3 scans are absent from the log. Guard the jq with type checks. | S | Malachi | idea (2026-08-24: prior promoted->AUDI-1203 was a bookkeeping error, that ticket is the whitelist audit) | AUDI-431 |
| IMP-041 | 2026-08-11 | airflow-ti — `export_tpa.py` `already_exported` must check `_SUCCESS`, not "any blob" | `already_exported` (line 127) is `list_blobs(prefix, max_results=1)` + `next(...) is not None`, so ANY single object short-circuits the export when `force=false`. A run that dies MID-write leaves partial parquet, and the next attempt skips it and reports success, shipping an incomplete ~400 GiB export green (same silent-degrade class as INC-013). Benign in INC-016 only because try 1 had finished. Fix: gate on the `_SUCCESS` marker (and/or compare part count). | S | Malachi (PR) / TPA_EXPORT (Sean / Brian) | **DONE** — [airflow-ti#1196](https://github.com/SteelHouse/airflow-ti/pull/1196) MERGED 2026-08-17 23:47Z, carried this gate PLUS the 3 remaining IMP-042 batch ids and the retry guardrail in one PR. Held OPEN at Sean's request for a Monday deploy. **DEV-VALIDATED 2026-08-17** on the dev deployment: (a) retries now mint distinct batches and do real work (`tpa-export-2026-08-15-1786992151-{1,2,3}`, created 7 min apart) where a control run on unmodified `main` created NO batch for try 2 and reattached; (b) happy path intact (`ipdsc` 24.7 min, `ipdsc_geo` ~7 min, both try 1); (c) the `_SUCCESS` gate stops a second writer: a fresh batch against a completed export ran 1m23s and wrote NOTHING (output stayed 559 objects / 12.79 GiB, `_SUCCESS` timestamp unchanged) vs 3m08s for the real export. Residual, unchanged from the 17 tasks that already suffix `try_number`: if a worker is SIGKILLed mid-batch the strand is not cancelled by `on_kill`, and a retry could write concurrently before any `_SUCCESS` exists. | on-call INC-016 |
| IMP-042 | 2026-08-11 | airflow-ti — fold `try_number` into the `tpa_export` / `ipdsc_geo` batch ids | Batch ids are minted by upstream `create_batch_id*` tasks and cached in XCom; a downstream retry re-reads the stale id and reattaches to the FAILED batch, so tries 2..N always die in seconds (INC-016 burned 4 retries). The id formula itself is already unique per call, so adding entropy there fixes nothing. The in-repo `ipdsc_ds_*` pattern (try_number folded in at point of use) is the fix. **Measured cost: every try-1 failure in the last 9 days became a page or a dead run (08/03 exhausted retries, dag run `failed`, never recovered; 08/06 and 08/10 needed a human).** ⚠ **Do NOT ship before IMP-041** — today the attach kills a retry before the export script runs, so it accidentally shields the `already_exported` partial-write bug; making retries real turns that into silent bad data. | S-M | TPA_EXPORT (Sean / Brian) | **DONE** — #1195 (2026-08-15) fixed the 2 materialize DAGs; [#1196](https://github.com/SteelHouse/airflow-ti/pull/1196) merged 2026-08-17 23:47Z fixed the last 3 `tpa_ipdsc_export` sites, with IMP-041 and a `RetrySafeDataprocCreateBatchOperator` guardrail that blocks a second concurrent batch. All 5 call sites now mint per-try batch ids. | on-call INC-016, INC-001, INC-012, INC-017 |
| IMP-043 | 2026-08-16 | airflow-ti — `wait_fpa` `timeout=900` is below the producer's real runtime distribution | Both `hashed_email_*_signals` consumers wait on `fpa_site_visit_batch_serverless` with a 15-minute budget, but that producer's dag run has a **median of 10.0 min and a 6% tail over 15 min** (99 runs since 2026-08-12; max 50.2 min). A slow-but-**successful** producer times both sensors out and pages twice. Worse, `AirflowSensorTimeout` fails the task outright and **skips retries entirely**, so `default_args={"retries": 1}` never fires and every occurrence needs a human clear. The resulting hourly hole never re-alerts: the 2026-08-13T05:00Z pair sat empty for 3 days until on-call found it by accident. Fix: raise `timeout` to cover the tail and/or wait on the producer's `all_batches_done` boundary instead of the individual `dsid*` task. Raising `retries` alone does nothing. | S | TGT (producer owner) / Malachi (PR) | **done** — [airflow-ti#1199](https://github.com/SteelHouse/airflow-ti/pull/1199) MERGED 2026-08-16 (approved), `timeout=900` → `1800` on both DAGs, DagBag-validated against a `main` control. Verified 2026-08-20: 240 `wait_fpa` runs across both DAGs since the merge, zero failures. | on-call INC-019 |
| IMP-044 | 2026-08-17 | airflow-ti — `site_network_hourly` runs with `retries=0` on a severity-1 hourly DAG | `TEAM.make_dag_args(severity=1, ..., default_args={})` leaves Airflow's default of 0 retries, so ANY transient kills the run and pages. INC-020 was a 15.9-second GCP 503 minting impersonated credentials, before the Dataproc batch was even submitted; one retry would have absorbed it silently. The 2-hour heal window means a single loss is benign, so this is pure alert noise, but two consecutive failures would leave a real hole with nothing to catch it. Fix: `default_args={"retries": 2, "retry_delay": timedelta(minutes=5)}`. Cheap: the failure mode is pre-submission, so a retry cannot double-submit a batch. | S | TPA_EXPORT / Malachi (PR) | **DONE** — [airflow-ti#1202](https://github.com/SteelHouse/airflow-ti/pull/1202) MERGED 2026-08-17 17:48Z, `retries=2` / `retry_delay=5min`, DagBag-validated against a `main` control. | on-call INC-020 |
| IMP-058 | 2026-08-19 | airflow-ti — give `ModelPysparkBatchOperator` an opt-in `_SUCCESS` skip gate | A retry recomputes a partition that is already complete. On 2026-08-19 `ipdsc_ds_35` try 1 finished its write (2051 objects / 520 GiB at 06:39:39Z, matching 08/16 and 08/17 exactly) and was killed 72s later by the worker recycle; Airflow never learned the outcome, so try 2 rebuilt the whole thing from BigQuery, ~2.5h of redundant Dataproc. The output already carries a `_SUCCESS` marker, so the check is cheap. `tpa_export` got exactly this in [#1196](https://github.com/SteelHouse/airflow-ti/pull/1196) and a second attempt there costs 83s instead of a full export. Make it opt-in per model with a force override, since some models must recompute. **Order: [#1206](https://github.com/SteelHouse/airflow-ti/pull/1206) first** — it removes the cause (task dying with its worker); this only reduces the cost when a retry still happens. | M | TI / Malachi | idea | on-call INC-021 |
| IMP-059 | 2026-08-19 | Databricks — turn on the workspace-wide *Enable auto flexible node types* toggle | [shopper_graph#300](https://github.com/SteelHouse/shopper_graph/pull/300) fixed ONE model. The toggle (Databricks workspace admin → Compute) applies fallback to every classic compute resource, including jobs nobody has audited. Cost objection is answered by the docs: auto fallbacks are capped at the same vCPU count and 100-110% of the preferred memory, so they cannot pick a beefier node, and billing is standard DBU rates for whatever is acquired. Residual exposure is only the per-hour delta between same-size families, on runs where the primary type is unavailable. Needs a workspace admin plus a nod from the cost owner. | S | AUDI / Databricks workspace admin | done 2026-08-19 | on-call INC-022; enabled after Alyson Lefkowitz signed off on reliability over marginal cost and Brian McAdams agreed conditional on cost monitoring. Applies to NEW classic compute only. Cost monitoring blocked on a `system.billing` grant. |
| IMP-060 | 2026-08-20 | airflow-ti / SteelHouse dbt — guard `keyword_ddp_reporting` against the `ddp_url_verticals` rebuild | `prod.ml.ddp_url_verticals` is a dbt table-materialized python model, so `create_ip_verticals / ddp_url_classification` (daily 00:05 UTC, 25 min to 2h32m) drops and recreates it. `keyword_ddp_reporting / write_targeted_signal_ds_13` reads it as a source with no cross-DAG dependency; the 9-hour schedule gap is the only thing keeping them apart, and a delayed reporting run collides. Either add a sensor on the producer or make the materialization non-destructive (swap into place). | S | mlsquad | idea | on-call INC-023 |
| IMP-062 | 2026-08-20 | Databricks — get `system.billing` readable so flexible node types can be cost-monitored | **PATH KNOWN as of 2026-08-25**: `system.lakeflow` was granted via metastore admin (catalog level) THEN full account admin (schema level), two passes by Alyson Lefkowitz. `system.billing` needs the same second pass; ask her. Check with `databricks api get /api/2.1/unity-catalog/permissions/schema/system.billing`. Turning on the workspace-wide flexible node types toggle was approved on the condition that someone watches costs, but our account has no `USE SCHEMA` on `system.billing`, so no baseline exists. A metastore admin needs to `GRANT USE SCHEMA ON SCHEMA system.billing` plus `SELECT ON TABLE system.billing.usage`. **Scope correction 2026-08-21, itself corrected 2026-08-24:** billing and lakeflow are in fact the SAME problem. Both deny on one gate — MNTN has no **metastore admin** assigned (post-Nov-2023 default; `databricks metastores summary` shows `owner: System user`), and granting on any `system.*` schema requires one. The 2026-08-21 reading of `lakeflow system schema can only be enabled by Databricks` as a Databricks-side wall was wrong: David Qiu (Databricks) confirmed lakeflow is enabled automatically and that error is expected. Assignment is console-only, must be a GROUP, metastore `c5dc6763-eaae-4d6c-9ae2-7af6147595bb`; then `USE CATALOG ON CATALOG system` + `USE SCHEMA`/`SELECT` per schema. Ask once, get both. Without it the monitoring commitment is a promise, not a measurement. | S | Databricks metastore admin | idea | on-call INC-022; IMP-059 |
| IMP-063 | 2026-08-20 | airflow-ti — revisit deferrable Dataproc polling after a runtime bump | The INC-021 fix for worker recycles was `deferrable=True` on `ModelPysparkBatchOperator`, dropped from airflow-ti#1206 because Airflow 3.1.5 cannot resume a `DataprocBatchTrigger` (proto-enum state breaks the triggerer's msgpack comms, apache/airflow#54836). Worker recycles remain unprotected. Re-test after an Astro runtime or `apache-airflow-providers-google` bump. | M | Malachi / TI team | idea | on-call INC-021; memory reference_airflow_deferrable_broken_3_1; asked Astronomer 2026-08-20 |
| IMP-064 | 2026-08-20 | credential inventory misses the scheduler layer | Auditing identities for `on-call/service_account_ask.md` found `SLACK_BOT_TOKEN` and `ANTHROPIC_API_KEY` in plaintext inside `~/Library/LaunchAgents/com.mntn.slack-knowledge-bot.plist`, still firing nightly ten weeks after that surface was decommissioned (2026-06-10). Agent unloaded and plist deleted; both credentials need revoking. The inventory in memory `project_deidentify_personal_credentials` only covered the repo, `~/.zshrc` and `~/.databrickscfg` — nothing looked at LaunchAgents or crontab. Fix = add a `launchctl list` + plist `EnvironmentVariables` sweep to `preflight.sh` or `verify.sh` so a scheduler entry holding a secret is caught locally instead of by GitHub push protection. | Found 2026-08-20 while building the service-account ask. GitHub push protection rejected the commit that would have archived the plist, which is how the secrets surfaced at all. | S | Malachi | idea | IMP-050 |
| IMP-065 | 2026-08-21 | airflow-ti — the debugger DAG needs an Airflow API token to read task logs | **DONE 2026-08-24.** Token minted by Ryan Kleck, stored in the macOS login Keychain as `astro_deployment_token` and resolved by `~/.zshrc` into `$AIRFLOW_BEARER` (same pattern as the Anthropic key). Verified live: the sweep diagnosed 7 of 7 failed tasks for 2026-08-20, 4 deterministically, and the run exposed a latent defect the tests could not (`page_limit`/`page_offset`, not `limit`/`offset`, or the POST 422s). Deployment half closed 2026-08-24: `AIRFLOW_BEARER` (secret) + `AIRFLOW_API_BASE` set via `astro deployment variable create`, deployment restarted, and a prod verification run diagnosed 7 of 7 failed tasks (4 deterministically) in 20.5s. **Rotation was raised twice and declined twice** — the token stays as-is by decision; if it is ever rotated, two places need updating (the Keychain entry and the deployment variable). `airflow_debugger_daily` (PR #1214) is the one workload the DAG move did NOT free from a credential. The optimizer dropped its Astro token because a DAG enumerates DAGs locally; task **logs** are different — on Astro Hosted they live in Astronomer's store, a task's own Task-Execution JWT is scoped to itself, and the deployment sets no `AIRFLOW__LOGGING__REMOTE_*` that would put them in a readable bucket. Set `AIRFLOW_BEARER` + `AIRFLOW_API_BASE` as secret deployment variables. Mint with `astro deployment token create --deployment-id cmd6bd10c0gl901rfuokgryiq --role DEPLOYMENT_ADMIN --expiration 365 --clean-output`. **Needs `WORKSPACE_OWNER`** (Ryan Kleck); there is no read-only role in the org, so DEPLOYMENT_ADMIN is the floor. Precedent: Ryan's `dag-run-duration-watchdog` token. | Not blocking: a missing token is a skip, not a failure, so the DAG merges and runs green without it and starts diagnosing the day it lands. | S | Malachi + Ryan Kleck | done | AUDI-1191 PR #1214 |
| IMP-066 | 2026-08-21 | GCP — identity for the debugger DAG | **DONE 2026-08-24 (mntn-devops#4990, merged + synced).** My Terragrunt PR #4985 was closed and rebuilt as Crossplane by Cristina Szumilo, who noted Crossplane self-syncs so nobody has to apply a plan by hand. All five project grants verified live; the two bucket grants and the `debugger/` condition survived the migration intact in the manifest. First framing was wrong: widening `spark-optimizer` would have turned a single-bucket cost-analysis identity into a cross-project log reader. A dedicated `airflow-debugger` GSA instead. The review also found the bigger gap: the optimizer SA holds ONLY `dataproc.viewer` and nothing in `mntn-targeting-prj-prod`, so the debugger had no `logging.viewer` (Dataproc driver text, its primary evidence) and no Vertex chain at all. New unit adds both plus the four targeting-project grants. Original note: The SA's `roles/storage.objectUser` carries an IAM condition scoped to the `optimizer/` prefix, so `airflow_debugger_daily` publishing to `gs://mntn-data-archive-prod/debugger/` will 403. Either widen the condition to both prefixes or give the debugger its own GSA from a copy of the 3-file `spark-optimizer` Terragrunt unit. **`objectUser`, not `objectCreator`** — creator cannot overwrite. | Not blocking: a publish failure warns and the diagnosis still runs, so the DAG is useful (via task logs) before the grant lands. | S | Malachi / mntn-devops | done | AUDI-1191 PR #1214; memory reference_gcs_iam_creator_vs_user |
| IMP-067 | 2026-08-24 | airflow-ti `mntn_match_verticals_precache_v1_1` + shopper_graph /vertical | **Fix or quarantine the 563-AID recurring scrape-failure population the vertical precache DAG re-hammers daily.** 97% of prod POST /vertical requests fail with 400 (2026-08-22: 622/636; 08-23: 626/643); 563 failing AIDs are identical across both days. Failure mode is scrape-returns-invalid-text (bot challenge / too-short) → self-selection fallback fails, not the SCRAPING FAILED exception. Every retry burns a scrape + fine-tuned-GPT call for an AID that has failed before, so the DAG's 30-min cron is mostly wasted LLM spend. Fix = skip AIDs that failed N consecutive runs (quarantine list) or fix the scraper's bot-challenge handling / backfill those verticals another way. Separate from AUDI-1086's endpoint work. | Found in AUDI-1142 prod-log evidence (2026-08-24): the precache DAG is the dominant /vertical caller (200 AIDs/run) and the failing set barely changes day to day. | M | DS team (Alyson) / Malachi flag | idea | AUDI-1142 `outputs/audi_1142_prod_log_evidence.md` |
| IMP-069 | 2026-08-24 | verified visits — root cause of the ~30 all-time untraceable rows | AUDI-802 closing per Zach Schoenberger's agreement (Slack 2026-08-24) with root cause unknown: ~30 stage-2/3 verified visits have no stage-1 impression even on an all-time trace (TTL and lookback ruled out), so a rare pipeline gap remains unexplained; the TI-650 audit tables were never created. Malachi wants this finished when bandwidth allows: build the trace tables, retest on a fresh cohort inside the 90d bid-log TTL, find the gap. Reopen sooner if untraceable VVs grow past rare or a billing dispute touches them. | M | Malachi | promoted -> AUDI-1222 (2026-08-25, Zach: build tables + DDM monitors) | AUDI-802, TI-650, Zach Schoenberger agreement 2026-08-24 |
| IMP-068 | 2026-08-24 | Python package distribution — Artifact Registry trigger | When a second consumer of the shared targeting-infra-ml package appears, file a DevOps request for a Python AR repo (the AR project already exists, repo creation is DevOps-gated) or extend the Fangorn GCS-wheel pattern TI-956 shipped (airflow-ti PRs 1064/1068/1073). Until then a dedicated registry has no consumer; AUDI-1023 closed obsolete on this basis. | S | Malachi | idea | AUDI-1023 backlog audit 2026-08-24 |
| IMP-050 | 2026-08-20 | Move all automation off Malachi's personal identity onto non-human identities | GCP ADC, the Astro bearer, Databricks (including a long-lived PAT sitting in `~/.databrickscfg`), `JIRA_API_TOKEN`, and `gh` all run as `malachi@mountain.com`. Every watcher and script is a bus-factor of one, breaks when a token expires mid-run, and attributes prod actions to a person. Must not reintroduce downloaded key files. **The pattern is settled and shipped as of 2026-08-21** (AUDI-1194's `spark_optimizer_daily`): move the workload into an `airflow-ti` DAG, copy the 3-file `spark-optimizer` Terragrunt unit, impersonate from the deployment ADC via `serviceAccountTokenCreator` (NOT `workloadIdentityUser`), and write GCS with `storage.objectUser` conditioned on the prefix (NOT `objectCreator`, which cannot overwrite). **Do not ship a container** — the Cloud Run/GAR/ArgoCD path was built and deleted, and moving the workload removed the Astro token dependency outright. Ask list: `on-call/service_account_ask.md`. | L | Malachi + whoever owns non-human identity at MNTN | idea | memory project_deidentify_personal_credentials |
| IMP-051 | 2026-08-20 | airflow_debugger / `dataproc_rca` — pin the token fetch too | **DONE 2026-08-20.** The pinned-IP Cloud Logging fallback resolves `logging.googleapis.com` via `dig @8.8.8.8` and pins curl to it, but `gcloud auth print-access-token` still goes through the system resolver. When the cached credential needs a refresh it hits `oauth2.googleapis.com`/`accounts.google.com` through the very sinkhole the fallback exists to route around, and the fallback dies at `access token: <err>` with sound pinning logic. Fixed by pinning the token host the same way: `_access_token()` tries `gcloud auth print-access-token` first and, only on a DNS-block marker, exchanges the ADC refresh token over a curl pinned at `oauth2.googleapis.com`. A non-DNS token failure (revoked, reauth required) is returned as-is and never retried, so a real credential problem is not masked as a network one. | Found reading the fallback during the 2026-08-20 live verification. Both driver-text paths returned real text on batch f73fa983 (20993 vs 20991 chars), so this is latent, not currently breaking. | S | Malachi (AUDI-1191) | done | AUDI-1191 DNS fallback verification |
| IMP-052 | 2026-08-20 | airflow_debugger / `dataproc_rca` — surface the real API error from the pinned curl | **DONE 2026-08-20.** The fallback runs `curl -s` without `--fail`, so an HTTP 4xx/5xx JSON error body parses fine, yields no `entries`, and the real message (quota, IAM, bad filter) is discarded and reported as the generic `no entries`. Fixed: `_api_error()` reads the response's own `error` object and returns `HTTP <code>: <message>`, so a 403 now reads `Permission logging.logEntries.list denied` instead of `no entries`. Applied to the Vertex pipelineJob GET too. | Same read-through as IMP-051; makes a future failure diagnosable instead of silent. | S | Malachi (AUDI-1191) | done | AUDI-1191 DNS fallback verification |
| IMP-053 | 2026-08-20 | on-call log acquisition — the retrospective pull selects on state AT PULL TIME, so it misses every failure that recovered | **DONE 2026-08-20.** `--state failed` reflects the task's state when the cron runs, not whether it ever failed, so any task that retried or was cleared to success between the alert and the next-day 10:00 PT run is invisible. That is most resolved incidents. **Correction:** this row first blamed tag coverage; that was wrong. All three 'missing' DAGs (`audience_intent`, `mntn_match_incrementals_fetch`, `keyword_ddp_reporting`) already carry `tpa` or `Machine Learning`. Verified against the live API: on 2026-08-19 nine tasks had `try_number > 1` and ended `success`, including all four INC-021 tasks and INC-022. Fix = `--include-recovered` on `airflow_api.py list` (selects `try_number > 1` regardless of final state, expands tries, and diagnoses the try that actually failed), wired through `airflow_pull.sh` and the daily cron. | Re-pulling 2026-08-19 went from 36 logs across 3 DAGs to 76 across 9, capturing INC-021 and INC-022 for the first time. Two taxonomy gaps surfaced immediately and were closed: an empty log on a `failed` task is worker death, not an upstream failure (INC-021), and `task_externally_terminated` for a try Airflow killed (INC-022 try 21). | S | Malachi (AUDI-1191) | done | AUDI-1191 corpus sweep 2026-08-20 |
| IMP-055 | 2026-08-20 | airflow_debugger — route Vertex and ExternalTaskSensor failures instead of trying to regex them | **DONE 2026-08-20.** The last 3 unclassified failure logs cannot be closed with a signature, because the only distinguishing text also appears in SUCCESSFUL runs: `fangorn_inference_pipeline_run/challenger_inference_pipeline` logs `Submitting Vertex AI Pipeline` + a Pipeline Run URL then dies with an empty exception, and `keyword_ddp_reporting/wait_for_product_categorization` logs `Poking for tasks [...]` then dies with no message. Matching either line would fire on every green run (the same defect that cost 325 false positives on `Waiting for the completion of batch job`). Fix = treat them like Dataproc: extract the Vertex pipeline run id from the Run URL as a job id and add a `vertex` engine branch; for the sensor, resolve the named external task's ACTUAL state via the Airflow API and branch on skipped vs failed (the INC-011 enrichment already specced in the ticket). Shipped as `vertex_rca.py` (pipelineJob → failed leaf → ml_job replica → nested Dataproc driver output, 5 layers, ~6s) and `external_task_rca.py` (the target's real state via the Airflow API). The sensor half re-exposed the IMP-053 defect: the API answers with the state NOW, so the target's `end_date` is compared against the moment the sensor gave up. INC-024 reads `vertex/model-alias-missing` at high confidence; the ValueError five layers down earned the one new signature, `model_alias_not_found`. | Closes the last 3 of 110 diagnosable failures: 101 classified, 9 routed, 0 unresolved, green-run hits unchanged at 5. More importantly it is the correct shape: a cause that lives in another system is a routing problem, not a taxonomy problem. 11 regression tests. | M | Malachi (AUDI-1191) | done | AUDI-1191 corpus sweep 2026-08-20 |
| IMP-056 | 2026-08-20 | targeting-infra-ml — a fangorn challenger run should fail fast on a missing model alias, and the registry should not silently lose one | Two gaps, one incident. (1) `run_challenger_inference.py` resolves `challenger-v*` only once the Dataproc cluster is up and the job is submitted, so a missing alias costs a full cluster create + 3 job retries + delete on every try, twice per Airflow run, to produce a one-line ValueError. Resolve the alias in the pipeline's FIRST component (or in the DAG) and fail before provisioning. (2) Re-registering `fangorn-hhid-xgboost` on 2026-08-18 dropped its `challenger-v*` alias with nothing noticing until the next challenger run three days later. Wanted: the registration step re-applies the aliases it replaces, or a check that every model with a scheduled challenger pipeline still carries a `challenger-v*` alias. | INC-024: hhid challenger green 08-12 and 08-17, model updated 08-18, every run since fails identically. The sibling `fangorn-xgboost` still has `challenger-v2`, so the convention and the code are correct; only the alias went missing. | S-M | Targeting ML (fangorn model owner) | idea | on-call INC-024 |
| IMP-054 | 2026-08-20 | airflow_optimizer — a durable register of optimizations that actually SHIPPED | **DONE 2026-08-20.** Extends the existing finding ledger rather than adding a parallel file. New `applied` state via `python3 -m airflow_optimizer.ledger applied <dag> <key> <pr> <date>`, which records the PR and applied date and carries both forward onto every later sweep, so the outcome stays attributable. **`applied` is deliberately not sticky:** a merged fix is not a verified fix, so the detectors decide what happened next. The finding going quiet becomes `resolved` (noted as 'cleared by <PR>'); the finding still firing after the grace window becomes `fix_not_working`. `ledger shipped` renders the register: applied date, DAG, finding, PR, outcome, and DCU/h before vs after. Cost is read per-DAG, not per-finding, because a finding that works stops firing and would otherwise have no 'after'. | User ask 2026-08-20. Without it the optimizer can only ever show potential value, and a re-crawl cannot tell an unfixed finding from a fixed-and-regressed one. Validated both directions end to end: a fix that worked reads 100.0 -> 40.0 DCU/h resolved, one that did not reads 80.0 -> 79.0 fix_not_working. 3 tests. | S-M | Malachi (AUDI-1194) | done | user request 2026-08-20 |
| IMP-047 | 2026-08-19 | MNTN Public MCP beta (`https://mcp.ex.mountain.com/mcp`). Validate its numbers against the warehouse before external GA | Benny opened the beta to eng on 2026-08-19 and asked for feedback before it rolls out externally. The server is customer-facing and read-only over advertisers, campaigns, audiences and reporting, so any metric it computes differently from silver is a defect a customer will see. We hold BQ ground truth plus the known-gotcha list (`ui_conversions.order_amt_usd` always NULL, `is_new` client-pixel mismatch, `objective_id` unreliable so stage comes from `funnel_level`, uniques collapse to ~0 in `agg__daily_sum_by_campaign` after Sep 2025, metro resolution per IMP-017), which almost no other beta tester can diff against. The quickstart also ships no tool inventory, so a user cannot tell what is callable without connecting. Server is registered in `.mcp.json`; the warehouse diff is blocked on the interactive OAuth handshake and on unconfirmed API access for the account. Eval done 2026-08-19: impressions, spend, visits and conversions match `sum_by_advertiser_by_day` **exactly** across 7 days for WGU, on the UI `industry_standard` lens (default + `competing_*`); reach is within HLL error. Two defects. (1) `run_report` with two dimensions whose short name is both `name` collapses them into one `Name` row key under the default `fullName:false`, silently dropping a dimension; `fullName:true` fixes it. Worst for an LLM client, which narrates the collapsed rows confidently. (2)  the authorization server publishes two discovery documents for the same issuer with two different `jwks_uri` and zero shared key ids (`mntn-auth-1`/`mntn-auth-2` vs `mntn-oidc-1`), so a token validated against the wrong set fails its signature. Both reported to Benny Poon 2026-08-19: (1) being fixed and deployed, (2) the second key set is deadweight, not a live risk. MCP `campaign.id` maps to our `campaign_group_id`, never `campaign_id` | S | AUDI (Malachi) / Benny, Daniella Kubiak | idea | `documentation/docs/mntn_public_mcp_quickstart.pdf`; `knowledge/memory/reference_mntn_public_mcp.md` |
| IMP-039 | 2026-08-10 | airflow-ti — dedicated prod backfill DAG for the feature store | Brian's ask (INC-015 retro thread): a paused prod DAG (schedule=None, Param(start,end,models)) for backfills/missing dates, so one-off patches never run through the live DAG or the whole-DAG UI backfill. Feature store is an ABT, historical adjustment is routine. Complements scripts/model_backfill.sh (dev+copy) for the run-in-prod case. | M | Brian McAdams / TI team | idea | on-call INC-015, Slack 2026-08-10 |
| IMP-040 | 2026-08-10 | airflow_api.py — auto-renew the astro bearer on 401 | **DONE 2026-08-20.** The astro SSO token expires ~1h (not daily as documented), so any long poller 401s mid-run. Any `astro` CLI call refreshes the token on disk: catch 401, shell out to `astro deployment list`, re-read `~/.astro/config.yaml`, retry once. Shipped in `_request`: a 401 triggers ONE renewal (`astro deployment list` re-mints the token on disk, then the config is re-read) and retries with the fresh value, which is also cached module-side so the caller's stale copy does not re-trigger it. **An explicitly supplied token is never renewable** (`--token` / `$AIRFLOW_BEARER`, e.g. a Deployment API token): swapping it for whatever sits in the personal astro context would silently undo the non-human identity. A 401 renewal cannot fix fails once instead of looping. Verified in all three directions. | S | Malachi | done | on-call INC-015 |
| 2026-08-06 | airflow-ti | Parallel model_run.py 409s on minute-resolution batch IDs (dataproc.py:133); local patch adds uuid suffix. PR upstream so parallel local runs work out of the box | done (airflow-ti#1180, with backfill runner) |
| 2026-08-07 | fangorn / AUDI-1049 | Shadow-parity readout for the household FS (HH-vs-IP audience sizes per vertical, coverage split, day-over-day household churn + reconciliation band). Was the AUDI-1170 gate for AUDI-1103 training; ticket closed with backfill only | idea |

| 2026-08-11 | idea | Index `documentation/docs/` — add `doc_type: reference` + `keywords:` front-matter to each file and extend `build_index.sh` to crawl `documentation/`, converting the docs shelf from pointer-only to grep-routable via `_ROUTING.md`. Follow-on to the CLAUDE.md slimdown (see `claude-prompts/workflow_audits/claude_md_slimdown_2026_08_11.md` step 17). | workflow |

| 2026-08-12 | idea | Rename `doc_type: bq_table` → `table` and `knowledge/bq/` → `knowledge/warehouse/` across the linters, `build_index.sh`, and every table doc. The kit ships a warehouse vendor's name as a built-in of a schema it presents as generic, forcing any non-BigQuery adopter to adopt a misleading label or diverge from the shipped linters. High blast radius (all table docs + 3 linters + the index builder), so it is a deliberate migration, not a doc fix. Found by the BLUEPRINT.md adversarial review. | workflow |

| 2026-08-12 | idea | `bootstrap.sh` hand-rolls its own dependency probes instead of calling `.claude/scripts/preflight.sh`, which ships in every bundle and is never invoked by anything. Two implementations, one dead. Either have bootstrap step 1 call preflight and keep only bundle-specific extras inline, or drop preflight from the packaged set. Found by the BLUEPRINT.md adversarial review. | workflow |
| IMP-045 | 2026-08-17 | airflow-ti / `guid_geos_summary_to_integration` + `spark/targeting/build_guid_geos_summary.py` | **Fix `geo.guid_geos_summary.count` so it means what the UI says it means, and keep some history.** (1) The Spark job reads up to 7 daily partitions and counts distinct IPs *per day*; the DAG then `SUM(count)`s those days into one `CURRENT_DATE` row, so a visitor active on five days is counted five times. Make it a single distinct-IP count over the window (or emit one row per source date and let the reader aggregate). (2) The DAG TRUNCATEs the table every run, so it holds one snapshot and cannot answer "when did this change". Keep N days of dated rows instead of truncating. | PS-8614: Apollo.io reported 89% "Other" in Audience > Site Visitors > Geography. Answer was non-US traffic (86%), not a defect, but neither question was answerable from stored data. Onset was uncheckable beyond the 8-day GCS retention on `guid_geos_raw`, and the ~1.03M "users" PS quoted is a 7-day sum, not unique visitors. | S | TGT (targeting) | idea | PS-8614; `knowledge/memory/reference_guid_geos_summary_pipeline.md` |
| IMP-046 | 2026-08-20 | airflow_optimizer / crawl ranking hides chronic mediums | `JobReport.score` ranks by (high, medium, total) per LOG, so a job whose every hourly run trips one medium finding sorts below a single job with two highs. `aug_log_ip_vertical_id_hourly` fired `shuffle_fetch_wait` at 31-45% on 11 of 11 runs on 2026-08-17 and never reached the top of the backlog. Rank by (findings per job across the window) as well as per-log severity, or add a chronic/recurring flag when the same detector+stage fires on N consecutive runs. | S | Malachi (AUDI-1194) | idea | AUDI-1194 daily-sweep review 2026-08-20 |
| IMP-061 | 2026-08-20 | airflow_optimizer / no map-side concentration signal | `shuffle_fetch_wait` cannot tell a genuine partition-sizing problem from a map-output concentration problem, and its stock fix (raise shuffle.partitions) is actively wrong for the second case. Proven on `site_network_hourly`: stage 9 waits 73% on 4.2M blocks while stages 29/35 wait 1s on 23.4M blocks of the same size; what differs is that stage 9's map output sits on 77 executors with one holding 24.6%, the others on ~430 with a 0.3% max. Add per-executor shuffle-write bytes to `StageMetrics` and emit the concentration in the finding. Fix text corrected in the meantime. | S-M | Malachi (AUDI-1194) | idea | AUDI-1194 verify pass 2026-08-20 |
| IMP-048 | 2026-08-20 | `spark-events` prefix has no lifecycle rule | `gs://mntn-data-archive-prod` has Delete rules for 14 prefixes but none for `spark-events/`, so the event-log archive grows unbounded: 17.1 GiB / 2,237 `.zstd` today at ~160 logs (~1.3 GiB) per day. The age-30 TTL was recorded as approved on 2026-08-04 but never applied; it needs `storage.buckets.update`, which I do not have. Low cost today, but the crawler now depends on this prefix so it should be governed, and 30 days is more history than the daily sweep needs. | S | Ryan / bucket admin (flag) | idea | AUDI-1194 daily-sweep work 2026-08-20 |
| IMP-049 | 2026-08-20 | stale `databricks-ti837` PAT in the macOS keychain | **DONE 2026-08-20.** The `[DEFAULT]` PAT stanza in `~/.databrickscfg` was removed 2026-08-20 and the token was dead server-side (`databricks tokens list` returns empty), but the keychain entry `databricks-ti837` still held it and `.claude/scripts/databricks_smoke.py` still read it. Entry deleted (`security delete-generic-password`, verified absent); the smoke test now builds its session from the `databricks` CLI OAuth profile via `Config(profile=...)`, with `$DATABRICKS_PROFILE` as the override so a service principal on OAuth M2M drops in without a code change. No token is read from disk or keychain anywhere in this workspace now. | S | Malachi | done | AUDI-1194 Databricks validation 2026-08-20 |
| IMP-070 | 2026-08-24 | targeting-ml / fangorn | **QA and prod share one regional N2 quota, and a QA run starved prod.** Give QA its own quota or its own project, or cap QA cluster size. Also stop the prod pipelines racing each other. The challenger cluster alone requests 4,672 of the 5,000 `N2_CPUS` in us-central1 (and 145,500 GB of `DISKS_TOTAL_GB`), so it starts successfully only when every sibling is down. Options: raise the regional quota, shrink the challenger cluster, or serialise `inference_pipeline`, `challenger_inference_pipeline` and the cross-DAG `fangorn_hhid_inference_pipeline_run`. | INC-025: both tries failed on `Insufficient 'N2_CPUS' quota. Requested 4672.0, available 328.0`. Settled by arithmetic: `fangorn-inference-26f05d0f`, created 22:44 by `vertex-ai-qa@`, is 1x n2-standard-32 + 290x n2-standard-16 = **4,672 N2 vCPU**, and 5,000 - 4,672 = 328 exactly. One QA cluster held the whole pool. QA runs the identical shape as prod and ran it four times that evening (19:59, 20:13, 20:27, 22:44); two cannot coexist under a 5,000 ceiling. The component retry (IMP-071 / PR #93) would NOT have saved this run, missing by ~4 minutes, so this row is the durable fix, not that one. **Deferred 2026-08-24 by Sean Yang to hackathon week, when Brian McAdams is back**, on the stated reasoning that there is no clear pattern. The 30-day base rate was measured after that call and was not in front of him: 7 `CreateCluster` refusals (`status.code=3`), on 07-27, 07-30 and 08-24, every one served to prod and none to `vertex-ai-qa@`. Sean agreed to both fixes on 2026-08-24 after seeing the arithmetic; Malachi set a reminder for **Tuesday 2026-08-25**, when Brian is back. Recurs whenever they overlap, which is whenever the sensors release together. **CLOSED 2026-09-02 — superseded, not done.** Brian McAdams: devops will not raise the ceiling and the cluster cannot shrink. The n2->n2d option shipped instead as a 3-family `instance_flexibility_policy` on both prod inference pipelines ([fe4e3d6](https://github.com/SteelHouse/targeting-infra-ml/commit/fe4e3d661245f7846223f6c9debd382e8627c517) + `0b19f29`, 2026-08-25), which spreads the same 4,672-vCPU request across `N2_CPUS`/`N2D_CPUS`/`CPUS` (~15,000 vCPU of headroom) without a quota request. `N2_CPUS` itself is still 5,000. Measured 2026-09-02 from the ClusterController audit log: 0 refusals (`status.code=3`) since 2026-08-24 22:58Z, and 0 stockouts (`code=14`) on `fangorn-inference-*`/`fangorn-challenger-*` since 2026-08-25 22:48Z against 27 in the two days before. Residual carried to IMP-099. | M | targeting-ml (Sean Yang / Brian McAdams) | **closed — superseded** | on-call INC-025, AUDI-1217 |
| IMP-071 | 2026-08-24 | targeting-ml / fangorn | **DONE 2026-08-24** — [targeting-infra-ml#93](https://github.com/SteelHouse/targeting-infra-ml/pull/93) merged (`bc60c8bd`), reviewed by Ryan Kleck, applied to all four pipelines carrying the helper. Make `_delete_cluster_before_retry` tolerate an absent cluster. When `CreateCluster` is refused nothing is created, so the cleanup `delete_cluster` raises `NotFound: 404` and that becomes the deepest exception in the replica log, hiding the refusal that actually stopped the run. Catch `NotFound` and re-raise the original error. | INC-025: the surfaced traceback said "Cluster ... not found", which reads as a deletion race. The real cause (quota) existed only in the admin audit log. | S | targeting-ml (Malachi) | done | on-call INC-025 |

- ENGINE-PROPOSE `c-2026-08-21-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-21-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-21-003` (knowledge_edit/costly_query): query shape run 2x billing 542.5 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-21-004` (knowledge_edit/costly_query): query shape run 1x billing 539.3 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-21-005` (knowledge_edit/costly_query): query shape run 2x billing 485.9 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-22-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-22-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-22-003` (knowledge_edit/costly_query): query shape run 2x billing 542.5 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-23-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-23-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-23-003` (knowledge_edit/costly_query): query shape run 2x billing 542.5 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-24-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-24-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-24-003` (knowledge_edit/costly_query): query shape run 2x billing 542.5 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)
| IMP-072 | 2026-08-24 | workspace / commit gate | **Harden `lint_comments.py` against its gauntlet findings, without growing it.** Seven confirmed defects remain open from pr_gauntlet run 3 (FAIL_MAX_ROUNDS): `#`/`--` lines inside multi-line shell/SQL strings count as comment blocks (would block a valid commit); `--staged` prints green (`nothing to check`, exit 0) when git itself fails; the top-of-file header exemption covers every blank-separated comment run before the first code line, not one usage header. The rest were defects in features the loop's fixers added and that were reverted with them (heredoc variants, `--all`/`--base`/`--prefix` flags, double `ast.parse`). Fix the three that apply to the committed 124-line version; do not re-add the reverted surface. | pr_gauntlet test run on f02f9a52 (2026-08-24): 4 rounds, 40 findings raised, 27 confirmed, fixer scope-growth reverted at f051f8e0 state. | S | workspace (Malachi) | idea | pr_gauntlet wf_b0607fbd-34a |
| IMP-073 | 2026-08-24 | airflow-ti / airflow_debugger | **Five live defects in the merged mask/publish PR (airflow-ti#1215), all execution-confirmed.** (1) `report.py:114` appends the "this is not the cause" mask note to every correctly-diagnosed `slack_notify_failed` verdict, telling the reader a right answer is wrong. (2) `vertex_rca.py:218` scans the whole 400-entry log blob, so a stale attempt-1 cleanup 404 overrides a genuine driver OOM verdict with the audit answer, silently. (3) `vertex_rca.py:226` re-captures the cluster name from raw JSON-escaped msgs, swallowing a literal `\n` into the name, so the CreateCluster audit lookup can never match on that path and reports "no audit entry" when one exists. (4) `daily.py:111` `_upload` raises FileNotFoundError when gcloud/curl is absent, crashing the sweep after diagnosis. (5) The "report announces any mask it cannot resolve" claim is unimplemented for Airflow-layer failures. Plus style debt: `_upload` re-implements `_access_token()`, ruff-format drift in `_upload`, dead `Mask.resolver` field. | pr_gauntlet report-only run on merged #1215 (wf_24d8c3b9-d7d, 2026-08-24): 12 confirmed findings, 0 refuted, each with an executed repro. The PR shipped green on ruff + 109 tests; none of them exercise the raw-msgs shape or the mask-note path. | M | AUDI-1191 (Malachi) | idea | pr_gauntlet wf_24d8c3b9-d7d |
| IMP-074 | 2026-08-25 | AUDI-1194 / `airflow_optimizer` | **Rank Databricks dbt work by real cost now that `system.lakeflow` is readable.** `job_run_timeline` where `run_type = 'SUBMIT_RUN'` is the only surface that sees ephemeral dbt submissions (1,505 rows in 7 days). Strip the per-run UUID suffix off `run_name` to get the dbt identifier as `prod-<schema>-<model>`, derive duration as `unix_timestamp(period_end_time) - unix_timestamp(period_start_time)` (the `run_duration_seconds` column is 0 on every row), and rank runs x duration descending. Query via `/api/2.0/sql/statements` on warehouse `14b311ac86ee2ca2`. Never join `system.lakeflow.jobs` for a name: ephemeral runs have no row there and the join silently drops 100% of them. | M | Malachi | idea | [[reference_databricks]]; unblocked by Alyson Lefkowitz 2026-08-25 |
| IMP-075 | 2026-08-25 | AUDI-1191 `airflow_debugger` | **PARTIALLY DONE 2026-08-25, and the original design does not work.** A task killed by a control-plane outage carries NOTHING about it in its own log: the heartbeat 500 and `psycopg2.OperationalError` live in Astronomer's API-server logs, not the task's. Verified on `audience_intent/wait_for_ipdsc_geo` (2026-08-19 06:39Z, inside the maintenance window) — zero matches for heartbeat, 500, OperationalError or pgbouncer. What IS detectable per-log: 22 pokes with ZERO reschedules means the sensor was polling in-process and the log stops mid-poke, i.e. the process was killed rather than the sensor timing out. That verdict now ships. The cross-log co-occurrence detector (N tasks across M DAGs dying within seconds) CANNOT be built on the current corpus: only ONE task from that window is captured, because `oncall_daily_rca.sh` pulls just the `tpa` and `Machine Learning` tags. Widening acquisition is the prerequisite, logged as IMP-081. **Signature for an Airflow control-plane outage.** A task killed because the Astronomer database went into maintenance shows no exception at all: the heartbeat `PUT .../heartbeat` returns `500 Internal Server Error`, backed by `psycopg2.OperationalError: server closed the connection unexpectedly` and PgBouncer `pooler error: server conn crashed?`. Verdict must be "not your DAG, re-run once the instance is back", never a code fix. Detect on the heartbeat/pgbouncer strings where present, and on a cluster of tasks dying within seconds with no exception where not. | M | Malachi | in progress | 30-day replay G3; Astronomer ticket #98048 |
| IMP-076 | 2026-08-25 | AUDI-1191 `airflow_debugger` | **DONE 2026-08-25.** A reschedule-mode sensor's log is a clean poke loop with no exception, because the `AirflowSensorTimeout` lands in a different try. The verdict now names the poke target and the poke count, and is distinguished from a killed process by whether reschedule lines are present. Pinned by tests in both directions. **Signature for a reschedule-mode sensor that ended `failed` with no timeout line.** `wait_for_challenger_features` produced a 216 KB log of poke lines ending on `Rescheduling task, marking task as UP_FOR_RESCHEDULE`; the `AirflowSensorTimeout` string the `sensor_timeout` signature needs is in a different try, so the classifier correctly finds nothing. Key on the poke loop plus the terminal state rather than the timeout string, and name the poke target. | S | Malachi | done | 30-day replay G2 |
| IMP-077 | 2026-08-25 | AUDI-1191 `airflow_debugger` | **DONE 2026-08-25, and the diagnosis was wrong.** These 8 logs are not a serialisation failure and not a missing Databricks handle: they are a `KubernetesPodOperator` whose pod never reached Running inside its 120s budget, so the operator deleted it and raised with an EMPTY message. Both `databricks_guid_geos/run_databricks_job` and `mntn_match_verticals_precache_v1_1/pre_cache_verticals` show the identical wait/delete pair. Detection is structural (budget announced + pod deleted + no root error), the pod name is extracted as the handle, and the verdict points at node capacity and image-pull time rather than the task's code. Pinned by 4 tests including both negative directions. **Verdict for an exception-less failure, plus a Databricks run-id extractor.** 8 logs end on `[error] task Task failed with exception` with no payload after it, so there is nothing to classify and no signature can help. Report "the exception did not serialise, fetch it from the owning system". `databricks_guid_geos/run_databricks_job` should yield a Databricks run handle and currently yields none. | M | Malachi | done | 30-day replay G1 |
| IMP-078 | 2026-08-25 | AUDI-1191 `airflow_debugger` | **DONE 2026-08-25.** `describe_failure_note()` reports an expired batch as expired, names a permission error as one, and `_scrub()` strips any account out of a CLI error before it can reach a published report. Pinned by 4 tests. **An expired Dataproc batch must not surface a raw gcloud error.** The `tpa_ipdsc_export` group routes correctly but the batch has aged out, and the report prints `ERROR: (gcloud.dataproc.batches.describe) NOT_FOUND ... This command is authenticated as malachi@mountain.com`. Unhelpful, and it leaks a personal identity into a published artifact. Detect NOT_FOUND on describe and say "batch expired, evidence no longer retrievable". | S | Malachi | done | 30-day replay G4 |
| IMP-079 | 2026-08-25 | AUDI-1191 `airflow_debugger` | **DONE 2026-08-25.** An empty stub now names the failing task. A stub's filename carries no run id, so the run is resolved from the log's day directory and matched on the stub's OWN state: `vertical_classification_api` had 21 runs on 2026-08-21 and matching on presence alone picked a green one. Pinned by 5 tests. **An empty stub should name the upstream task that failed.** 51 of the 206 failed-state logs are 49- or 69-byte stubs where the task never ran. The debugger correctly says so, but the reader's next question is always "which upstream task", and the Airflow API can answer it from the same dag_run already being fetched. Highest-volume reader complaint in the corpus. | S | Malachi | done | 30-day replay 3a |
| IMP-080 | 2026-08-25 | AUDI-1191 `airflow_debugger` | **DONE 2026-08-25, same day.** `db_credential_rejected` signature: a database rejecting the credential itself (`PSQLException: FATAL: password authentication failed`, MySQL `Access denied for user`, `ORA-01017`, `Login failed for user`) is not the same fault as a missing IAM grant, and `auth_error` did not match it. Found live on `bottom_up_keywords_pipeline_run/training_pipeline`, five layers down a Vertex code-9. Ordered before `auth_error` so the specific case wins. | S | Malachi | done | live alert 2026-08-25 |
| IMP-081 | 2026-08-25 | on-call acquisition | **Widen log acquisition beyond two tags.** `oncall_daily_rca.sh` and the debugger DAG pull only `tpa` and `Machine Learning`, so a control-plane outage that killed four tasks across three DAGs shows up as ONE log in the corpus and the co-occurrence signal is invisible. Prerequisite for IMP-075's cross-log detector. `airflow_pull.sh` already supports an unfiltered pull since the `set -u` fix on 2026-08-25. | S | Malachi | idea | 30-day replay G3 |
| IMP-082 | 2026-08-25 | AUDI-1191 `airflow_debugger` | **DONE 2026-08-25.** `_resolve_profile()` reads `$DATABRICKS_PROFILE`, else the first non-DEFAULT section of the caller's own `~/.databrickscfg`, so the package works for anyone without naming a human. README updated. Two tests pin it: the source carries no `@mountain.com`, and `DEFAULT` is never selected. **Drop the hardcoded personal profile from the workspace package too.** `databricks_rca.py:22` is `PROFILE = "malachi@mountain.com"` and `README.md:42` names the same account. The BUNDLE copy was already fixed (reads `$DATABRICKS_PROFILE`, no default, skips the Databricks layer when unset), so this is the laptop half only and nothing in prod carries it. Still worth removing: it is the last place the tool assumes one human, and it blocks anyone else running the package. Mirror the bundle's env-var pattern. | S | Malachi | done | AUDI-1191 PR #1214 self-review defect 1 |
| IMP-083 | 2026-08-26 | airflow_optimizer / `cache_ineffective` has never been exercised | The detector fires on `run.rdd_evictions > 0`, and across a random 300-run sample of the 30-day archive `cached_rdd_bytes` is 0 on every run: nothing in the fleet caches an RDD, so the check has never had a case to judge and is unproven in either direction. Every other silent detector was confirmed live with its input present (GC time recorded, max 4.3% vs a 10% threshold; executor removal reasons recorded with no spot/preempt string; 0 FetchFailed tasks). Either build a fixture that exercises it or retire it, but do not report it as "working" on the strength of silence. | S | Malachi (AUDI-1194) | idea | AUDI-1194 full-corpus validation 2026-08-26 |
| IMP-084 | 2026-08-26 | airflow_optimizer / `shuffle_fetch_wait` cannot be ranked against cost | The check divides fetch-wait time by task time, both sums over tasks, so a stage that does little compute reports a huge share of a tiny denominator. On `site_network_hourly` it reported 57-90% of task time on stage 9 while that stall was a median 0.28% of the run's executor-hours; the same job's idle executors were 86% of its 21,200 executor-hours and ranked below it. Carry the absolute hours on the finding and gate the impact tier on them, so a ratio-based check cannot outrank an hours-based one. **DONE 2026-08-26.** `OptFinding.cost_h` now carries the executor-hours at stake and `_gated()` denies `high` to any ratio finding worth under 10% of the run's own executor-hours. Verified on `app-20260825065124173-0803`: idle 346.2h stays high, stage 9's true 68% fetch wait is 0.6h and drops to medium. | M | Malachi (AUDI-1194) | done | AUDI-1194 mechanism pass 2026-08-26 |
| IMP-093 | 2026-08-27 | airflow_optimizer / spark reference | **Pick the canonical source for Spark fix guidance.** It now exists in three places: the hardcoded `fix=` strings in `optimizations.py` (AUDI-1194), the hackathon list's trap notes (`outputs/audi_1194_hackathon_optimizations_2026_08_27.md`), and the new `pyspark-optimization-databricks-dataproc` skill + `reference_pyspark_optimization_skill` annex. Declare one canonical copy per class of guidance and make the others point at it, or the three will drift. | S | Malachi | idea | skill install 2026-08-27 |
| IMP-098 | 2026-09-02 | targeting-ml / fangorn | **`fangorn_conversions_training_pipeline.py` never got the flexible-worker fix.** It still hardcodes `machine_type_uri: n2-highmem-16` on 1 master + 16 workers, and its `create_dataproc_cluster` component pins `google-cloud-dataproc==5.4.0` — `instance_flexibility_policy` is absent from `InstanceGroupConfig` in 5.4.0, so adding the policy without also bumping the pin drops the field silently. Fix = add the policy AND bump to 5.10.1, mirroring `fe4e3d6`. | It took the only fangorn stockout since the 2026-08-25 fix: `fangorn-conversions-training-8012099b`, `status.code=14`, 2026-09-01 21:07:56Z. Alex Knorr shipped the same policy to the sibling `fangorn_conversions_inference_pipeline.py` in `7247996` the same day and did not carry it to the training file. Small cluster (16 workers), so low blast radius. | S | targeting-ml (Alex Knorr) | idea | AUDI-1217 §8 |
| IMP-099 | 2026-09-02 | targeting-ml / fangorn | **`DISKS_TOTAL_GB` is the residual QA-starves-prod path and the flexibility policy cannot spread it.** Disk quota is machine-family-agnostic, so the 3-family worker pool gives it nothing. One full-shape cluster is 1x500 GB master + 290x500 GB `pd-standard` workers = 145,500 GB (matches the `Requested 145500.0` in the INC-025 refusal exactly); two concurrent clusters need 291,000 GB against a 247,808 GB limit and are still refused by ~43,192 GB. Cheap guards if it recurs: smaller `boot_disk_size_gb` on workers, or QA on a reduced shape. | INC-025 recurs whenever a QA and a prod run overlap. `vertex-ai-qa@` still creates full-shape clusters in the same project (`fangorn-challenger-883bbd2b` 2026-08-26 13:19Z). It has not recurred in the 8 days since the fix because the windows have not collided, not because the shared-pool condition was removed. Watch signature: `status.code=3` naming `DISKS_TOTAL_GB` with `N2_CPUS` absent — that means the CPU half is working and only disk is binding. | M | targeting-ml (Brian McAdams) | idea | AUDI-1217 §4.5 |
| IMP-100 | 2026-09-02 | ai-workflow-kit | **`bq_run.sh` appends to `knowledge/bq_perf_log.jsonl` with no lock.** Wrap the append in `flock` (or write one file per run and merge) before parallel agents become routine. Line 195 is a bare `echo "$LOG_ENTRY" >> "$LOG_FILE"`; under N concurrent ticket agents each running BigQuery, N writers share one append handle. | Single-line `>>` appends under `PIPE_BUF` are atomic on macOS, so nothing has interleaved yet and no corruption is observed — the exposure is new as of `/sprint` (2026-09-02), which is the first thing that runs many BQ-capable agents at once. A long log entry crossing 4 KB would be the first failure. | S | ai-workflow-kit (Malachi) | idea | /sprint design, [[reference_sprint_skill]] |
| IMP-101 | 2026-09-02 | ai-workflow-kit | **`lib/mntn_xlsx.py` carries 23 pre-existing multi-line comment blocks, so the `lint_comments` commit gate blocks any change to it.** Decide per block: keep the one load-bearing line, or move the paragraph into the module docstring / this file. | The blocks encode real gotchas (openpyxl re-reads an image ref on every save so BytesIO breaks; a bare 6-hex tab colour stores alpha 00 and renders as no colour), so bulk-deleting them destroys hard-won context. Found while adding header and tab-name checks on TI-1313; that commit used `--no-verify` rather than rewrite 23 unrelated blocks. | M | ai-workflow-kit (Malachi) | idea | TI-1313, `documentation/docs/xlsx_deliverable_standard.md` |
| IMP-102 | 2026-09-03 | airflow-ti / ipdsc | **Broadcast the small join side in `ipdsc_ds_67` and cache `upload_ips`.** One-line change at `models/ipdsc/ipdsc_ds_67.py:80`: `ips.join(F.broadcast(audience_upload_ids), on="data_source_category_id", how="inner")` (`F` already imported at line 7); second, `upload_ips` is scanned twice (stages 3 and 5), so `.cache()` it or make it one pass. | Mechanism from app-20260805053727558-0431: stages 3 and 5 each explode `data_source_category_ids` over the 10.6 GiB DS4 scan and write an 81.6 GiB sort-merge exchange (129 GiB memory / ~80 GiB disk spilled each) to join `ui.audience_uploads` over JDBC; AQE then converts the join to `BroadcastHashJoin BuildRight`, so the small side is broadcast anyway after the shuffle cost is paid. `spark.sql.files.maxPartitionBytes` cannot help (60 MiB single-row-group input; AUDI-1273 D1 Option A, ledger keys `disk_spill:3`/`:5` set `wont_fix`). ~9.4 exec-h/day on the 09-02 sweep; would also address `shuffle_fetch_wait:13/37`, `skew:23`, `straggler:37` on the same stages. Size guard: AQE only broadcasts a side under the 10 MB adaptive threshold, so record the broadcast size from the plan metrics on the first run after the change. | S | Alyson Lefkowitz (model author, last commit 2026-08-04); Malachi to hand over | idea | AUDI-1273 §8.4, AUDI-1290, `knowledge/decisions/0003_ipdsc_ds_67_max_partition_bytes_dropped.md` |
| IMP-103 | 2026-09-03 | data quality / conversion_log archive | **Spike: `conversion_log` GCS archive volume drop 2026-08-20.** Establish whether `gs://mntn-data-archive-prod/conversion_log/dt=<date>` falling ~6x is an intended upstream change or a loss; first check = BQ `logdata.conversion_log` daily COUNT(*) across 08-19/08-20, then the job that writes the prefix. | 18-23 GiB/day on 08-18 and 08-19, 3-4 GiB/day from 08-20 with no recovery through 08-31; downstream `feature_store/feature_group_1_source/conv_log_ip` fell 500-750 to 72-89 MiB/day; no commit to `conv_log_ip.py`; consumers are feature store group 2 (`conv_log_derived_ip` and downstream) and Fangorn. Flagged to the user on AUDI-1273 (D2), nothing opened. Side effect: the `conv_log_derived_ip disk_spill:1` ledger key is shrinking on its own and may resolve by ~09-19 attributed to the AUDI-1273 PR. | S | Malachi (Spike under AUDI on a yes) | idea | AUDI-1273 §8.5, `knowledge/data_knowledge.md` § conversion_log GCS archive volume drop |
| IMP-104 | 2026-09-03 | airflow-ti / site_network_hourly | **`site_network_hourly` loses whole hours to `FetchFailed` storms and still reports SUCCEEDED.** Two defects: (1) dynamic-allocation scale-down decommissions executors whose shuffle blocks live jobs still need (`shuffleTracking.timeout=300s`), so stages re-submit up to 116 times and both hour jobs can die at the 4-attempt cap; (2) `models/bidstream_hourly/site_network_hourly.py` L245-250 swallows each hour's exception, so the batch SUCCEEDS with 0 bytes or a partial `hh=` dir without `_SUCCESS`. Fix direction: surface the failed hour (fail the task or alert) and keep scale-down from dropping live shuffle output. | `app-20260901205120829-0863` ran 2 h 25 min with 8,244 FetchFailed task ends, wrote 0 bytes, batch SUCCEEDED; `dt=2026-09-02/hh=05` left with 29 files and no `_SUCCESS` (`app-20260902085134727-0157`). Baseline 08-27..09-02: 167 hour partitions, 166 complete. Found on AUDI-1275 while baselining the speculation canary (#1271); speculation cannot repair a lost shuffle, so it is not that ticket's fix. | M | Malachi (ours since #1232) | idea | AUDI-1275 §4.6 / §8, `knowledge/data_knowledge.md` § site_network_hourly SUCCEEDS with a missing or partial hour |
| IMP-105 | 2026-09-03 | airflow-ti / attribution tests | **`tests/test_url_pattern_pipeline.py` sets `REPO_ROOT = Path(__file__).resolve().parents[2]`, one level above the repo, so `test_url_and_dlv_dag_calls_pass_shared_location[url\|dlv]` fail with `FileNotFoundError` on main and their AST assertions (the DAG files pass `location=BQ_LOCATION`) never run.** Fix = `parents[1]`, then act on whatever the two assertions reveal. | Pre-existing on main `825b07e` (20 passed / 2 failed), left byte-for-byte untouched by airflow-ti#1278 (scope). Root `tests/` is not in CI, so nothing flags it. Found on AUDI-1278. | S | Malachi (one-line test fix) | idea | AUDI-1278 §4.2 / §8.6, [[reference_airflow_ti]] |
| IMP-106 | 2026-09-03 | airflow-ti / aug_log_ip_vertical_id_hourly + site_network_hourly — feature_store / bidstream_hourly | **Bundle tldextract in `utils_model.zip` or as a wheel on `ti_resources` to remove the runtime `pip install tldextract` prologue that idles 50-100 executors for 23s-38min per run.** Aug_log driver payload: 113 of 255 registered executor-hours (44% of the run) spent idle during the pip install; site_network holds 2 executors for a 17-min prologue (owner question: what does the driver do). Both models call `_ensure_tldextract_on_executors` (L21-40 in aug_log, L5-24 in site_network) synchronously before the Spark job. Sizable lever (~58 DCU-hours per aug_log run, per-run multiplier 0.5-45 executor-hours depending on the prologue length). The initialExecutors knob (AUDI-1271) cannot remove this cost because it applies during the prologue, before any task runs. | INC-005 / AUDI-1271 (§4.4 new finding): the biggest aug_log cost is the driver-side prologue, measured across 20 runs (2026-08-31 to 2026-09-03 UTC). Verified from event logs: first task launches 23s to 2,286s after app start, median ~2.4 min; the three longest prologues (run 0790, 0707, 0053) are the three heaviest batches, with 90%+ executor-hours idle during the prologue. | M | feature-store owner (Malachi to hand off) | idea | AUDI-1271 §4.4, [[project_airflow_optimizer]] |

- ENGINE-PROPOSE `c-2026-08-25-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-25-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: doc_debt] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-25-003` (knowledge_edit/costly_query): query shape run 1x billing 586.2 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-26-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-26-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-26-003` (knowledge_edit/costly_query): query shape run 1x billing 586.2 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- IMP-082 | debugger | Sync `include/airflow_debugger/` in airflow-ti with the workspace copy before the Slack token lands. The bundle carries the pre-fix `slack_block.py`, which posts "no cause found" on 36% of failure volume. | Status: idea | Ref: AUDI-1191 replay 2026-08-26
- IMP-086 | workflow kit | pr_gauntlet.js resume replays a cached fixer's REPORT without re-applying its EDITS, and the run start resets the review files, so a resumed run silently reviews the pre-fix tree and self-declares THRASH. Persist fixer diffs (git stash or patch file keyed to the run id) and re-apply on resume, or refuse resume past a completed fixer. | Status: idea | Ref: AUDI-1194 PR 2 gauntlet 2026-08-26
- IMP-087 | debugger | notify.find_alert_ts reads only the first conversations.history page (100 messages), so on a busy day the alert can sit past the cut and the reply posts loose instead of threaded. Add cursor pagination bounded by oldest. | Status: idea | Ref: pr 1230 gauntlet rejected-as-out-of-set finding 2026-08-26
- IMP-088 | debugger | RESOLVED 2026-08-27 (user decision): prod path stays LLM-free; root-causing is ~100% deterministic after the 30-day backfill closed the taxonomy. LLM use stays on-demand on the Mac (synth.py) to DRAFT signatures for new unclassified shapes, which then land via reviewed PR. No key added to the prod path. | Status: closed | Ref: 30-day backfill 2026-08-27
- IMP-089 | debugger | dag_run_duration_watchdog success logs quote OTHER DAGs' failure text: 20 body-vs-filename identity disagreements and a share of the 105 green-run signature fires in the 2,924-log sweep. Contained (only failed tasks are diagnosed) but the sweep's false-positive check now needs a watchdog exclusion to stay meaningful. | Status: idea | Ref: corpus sweep 2026-08-27
- IMP-090 | debugger | 8 diagnosable failures classify to nothing, clustered on databricks_guid_geos run_databricks_job "[error] task Task failed with exception" with an empty payload. Candidate: engine routing via the job link rather than a new regex. | Status: idea | Ref: corpus sweep 2026-08-27
- IMP-091 | debugger | test_a_caller_supplied_run_id_reaches_the_walk subprocesses the real airflow_api.py, so it passes or fails based on ambient astro credentials (live 404 on the fixture run_id). Mock the walk's API seam so the suite is hermetic. | Status: idea | Ref: night run 2026-08-26
- IMP-092 | debugger | Delivery is at-least-once, not exactly-once: a crash after some posts but before publish re-posts the delivered diagnoses on retry. Exactly-once needs per-post dedup in notify (own-post detection per diagnosis, the _OWN_POST_PREFIX machinery). Gauntlet-rejected as out-of-set on PR 1230. | Status: idea | Ref: 1230 round 2026-08-26

- ENGINE-PROPOSE `c-2026-08-27-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-27-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-27-003` (knowledge_edit/costly_query): query shape run 1x billing 586.2 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)
- IMP-094 | optimizer | Profile the 39 non-Spark DAGs (coverage report 2026-08-27): per-operator cost surfaces — BQ tasks via INFORMATION_SCHEMA slot usage, Databricks jobs via system tables already granted, K8s pods via requested-vs-used resources. Extends the optimizer beyond Spark event logs; sized a few days, good hackathon item. | Status: idea | Ref: spark-optimizer digest 2026-08-27
- IMP-095 | debugger | Rapid debugger misses alerts whose failure end_date falls outside the 45-min lookback when cycles pause (deploy rollouts, env-var restarts); fangorn_household_14day_lookback 2026-08-28 went terminal with no thread reply. Fix idea: persist a last-successful-cycle watermark and look back to it. | Status: pr_open (airflow-ti#1249: cycle_watermark.json markers, lookback to last completed cycle capped at 6h; closes on merge) | Ref: live validation 2026-08-28, PR 1249
- IMP-096 | debugger | Cross-DAG root-cause walk: the 2026-08-29 10:31 sensor reply stopped one layer short at the sensor instead of walking to the missing GCS path two DAGs up (the dead OpenAI cohort). Extend external_task_rca to follow the producer chain until it lands on the artifact/task that actually broke. | Status: pr_open (airflow-ti#1249, partial: fast-fail sensors now reach the API-state verdict and the upstream walk; the producer-chain walk is still open) | Ref: missed_replies 2026-08-29, incrementals recovery, PR 1249

- ENGINE-PROPOSE `c-2026-08-28-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-28-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-28-003` (knowledge_edit/costly_query): query shape run 1x billing 586.2 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-29-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-29-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-29-003` (knowledge_edit/costly_query): query shape run 1x billing 586.2 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-30-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-30-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-30-003` (knowledge_edit/costly_query): query shape run 1x billing 586.2 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-31-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-31-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-08-31-003` (knowledge_edit/costly_query): query shape run 1x billing 586.2 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)
| 2026-08-31 | filed AUDI-1301 | Move OpenAI batch pipeline out of Default project into a dedicated project with audit logging enabled (Alyson enables on move); new project-scoped key + Astro secret swap; perms group api.admin / organization.write / spend_limits.read for Brian/Sean/Ryan/Malachi | AUDI-1191 outage: no audit log existed to diagnose the 08-28 org-side failure |

- ENGINE-PROPOSE `c-2026-09-01-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-09-01-002` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-09-01-003` (knowledge_edit/costly_query): query shape run 1x billing 586.2 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-09-02-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-09-02-002` (knowledge_edit/costly_query): query shape run 1x billing 3195.6 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-09-02-003` (knowledge_edit/costly_query): query shape run 1x billing 620.7 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-09-03-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-09-03-002` (knowledge_edit/costly_query): query shape run 1x billing 3195.6 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-09-03-003` (knowledge_edit/costly_query): query shape run 1x billing 2212.1 GB total — document a cheaper path or a materialized source [metric: doc_debt] — AWAITING APPROVAL (rung above current auto tier)
| IMP-107 | 2026-09-03 | airflow_debugger + spark_optimizer / adoption | **Make the debugger and optimizer adoptable by any team at MNTN, not just AUDI.** Today both are wired to this squad: `OPTIMIZER_BQ_SAS` is our service account, `phs.TEAM` is our batch label, the Slack channels, the Jira project and the triage epic (AUDI-1054) are all ours, and nothing states the prerequisites a new team must satisfy before their first sweep. Deliverable is a setup path a stranger can follow end to end: what access to request (GCS event logs, BigQuery INFORMATION_SCHEMA, Dataproc, Databricks `system.billing`, a Slack app, a Jira service account), which env vars to set, what each one changes, and what a correct first run looks like. Blocked-on-nothing; this is the gate on sharing it company-wide. | Raised by Malachi 2026-09-03 after the camperbid #580 incident showed the tool reaching outside its own team by default, and after Robin flagged that broadly-used tooling needs a service account rather than a personal token. | L | Malachi | idea | [[project_airflow_debugger]], [[project_airflow_optimizer]], [[reference_astro_deploy_mechanics]] |
| IMP-108 | 2026-09-03 | airflow_debugger + spark_optimizer / reply quality | **Systematically review and test every reply shape both tools emit, rather than fixing them one screenshot at a time.** The last three reply defects (wrapper replies that pointed at the log instead of reading it, manual re-runs going unanswered, the raw-traceback Why) were all caught by a person noticing a bad post in Slack. Deliverable is a corpus of real failures per signature class with the rendered reply pinned in a test, so a regression in wording or structure fails CI instead of appearing in the channel. | Malachi 2026-09-03: "we probably need to test everything, optimize all the replies." Precedent: AUDI-1191 #1268 and #1285 both shipped after a screenshot, not after a test. | M | Malachi | idea | AUDI-1191, [[reference_slack_debugger_app]] |
| IMP-109 | 2026-09-03 | airflow_debugger + spark_optimizer / LLM layer | **Add an LLM layer that proposes the actual fix, on top of the deterministic structure both tools already produce.** The signature catalog gives a correct but generic remedy; a model reading the same evidence could name the specific change. Constraint that has to be designed around first: the deployment is deliberately key-free, and a local API key is the pattern MNTN decommissioned with the Slack bot on 2026-06-10, so this needs an approved key story on Astro (or a vendor path) before any code. Keep the deterministic answer as the labelled floor and mark model output as unverified, per the existing Why-source precedence. | Malachi 2026-09-03. The `why()` precedence already reserves a WHY_LLM slot below the evidence sources, so the shape exists; what is missing is the credential path and the evaluation. | L | Malachi | idea | [[project_airflow_debugger]] `why()` precedence, `.claude/CLAUDE.md` no-API-key-on-servers rule |
| IMP-110 | 2026-09-03 | spark_optimizer / findings | **Drive the optimizer's open findings to zero: work every finding it reports, not just the ranked top of the digest.** The 2026-09-03 sweep carries hundreds of open findings across the fleet (ipdsc_third_party_audience_builder alone has ~25). The hackathon shipped seven PRs against a handful of them; the rest have never been attempted. Deliverable is a campaign that either fixes, wont_fixes with a reason, or reassigns every open key, so the backlog reflects real remaining work instead of accumulated noise. Depends on the ledger manifest (#1284) so each fix is attributed and measured. | Malachi 2026-09-03: "run something where we attempt to resolve every single possible thing the optimizer scans for." | L | Malachi | idea | AUDI-1290 epic, [[project_airflow_optimizer]] |

- ENGINE-PROPOSE `c-2026-09-04-001` (knowledge_edit/costly_query): query shape run 2x billing 5366.8 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-09-04-002` (knowledge_edit/costly_query): query shape run 1x billing 3195.6 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)

- ENGINE-PROPOSE `c-2026-09-04-003` (knowledge_edit/costly_query): query shape run 1x billing 2908.4 GB total — document a cheaper path or a materialized source [metric: usd_per_query] — AWAITING APPROVAL (rung above current auto tier)
| IMP-111 | 2026-09-04 | airflow-ti / spark_optimizer ledger | **28 ledger rows dated 2026-09-03 are stamped `resolved` and are not.** The 2026-09-04 sweep resolved them after two quiet sweeps instead of three, and the digest reported them cleared. AUDI-1326 / PR #1286 stops new ones but is forward-only by design. Deciding whether to un-stamp them is a deliberate data edit on an append-only ledger: options are leave them (wrong but inert, and the keys re-fire on their own), or append a correcting row per key. | Replay of the live 1,692-row ledger reproduces exactly 28, matching the 28 rows in the published file. All 28 last fired 2026-09-01. | S | Malachi | idea | AUDI-1326 §2, [[project_airflow_optimizer]] |
| IMP-112 | 2026-09-04 | airflow-ti / spark_optimizer ledger | **`prior_sweep_dates` is computed across all surfaces, so a BigQuery, Databricks or pod key can be resolved from sweep dates on which only the Spark surface recorded.** Pre-existing, untouched by AUDI-1326. The next resolution-semantics defect; name it before someone rediscovers it from a Slack digest. | Found reviewing AUDI-1326; flagged by two independent refuters. `sweep.run()` calls `record()` once per surface with the same date. | S | Malachi | idea | AUDI-1326, [[project_airflow_optimizer]] |
| IMP-113 | 2026-09-04 | airflow-ti / spark_optimizer ledger | **A manual `mark_applied` run AFTER a sweep on the same date leaves that dag-day with no measurement.** `append()` dedups on `(date, dag_id, key)`, so the applied row replaces the sweep row, and since AUDI-1326 stopped copying `exec_h` forward the replacement carries no hours. `sweep.run()`'s own order (`apply_manifest` before `record`) is safe; only the `__main__` / manual CLI path is exposed. Not guarded by a test. | Raised by a refuter on AUDI-1326 and waived there as out of scope: fixing it means changing `append()`'s dedup contract, which the retry-idempotence work also depends on. | S | Malachi | idea | AUDI-1326, [[project_airflow_optimizer]] |
| IMP-114 | 2026-09-04 | airflow-ti / spark_optimizer | **`optimizer_stage_metrics.jsonl` is not being restored between sweeps.** The 2026-09-04 task log says `stage metrics not present, starting fresh` although the GCS object exists, and the file the run then published holds exactly one sweep's output. The feature is one day old, so a genuine first publish is also consistent with this; the bucket exposes no earlier generation to compare against. Worth one look before the regression guard (AUDI-1281) depends on the history. | Noticed during the AUDI-1326 verification pass; explicitly left unresolved there. | S | Malachi | idea | AUDI-1326, AUDI-1281 |
| IMP-115 | 2026-09-04 | airflow-ti / airflow_debugger | **`dataproc_rca.py:40` sets `_LOG_FRESHNESS = "45d"` but Cloud Logging keeps Dataproc driver logs 7 days.** `gcloud logging buckets list --project=mntn-prj-prod-00` shows `_Default` `retentionDays=7`; the 400-day bucket is `_Required`, admin audit only. The constant is 6.4x wider than the data that exists, so the "no driver log via Cloud Logging (check freshness window)" note misdirects whoever reads it. Either correct the constant to 7d, or raise the log-bucket retention if driver logs are worth keeping (they are: 22 of 27 failed batches were already unrecoverable when AUDI-1327 went looking). | Probed on AUDI-1327: of 27 distinct failed Dataproc batches, only 5 returned any text, oldest survivor 2026-08-29. | S | Malachi | idea | AUDI-1327 §8, [[project_airflow_debugger]] |
| IMP-116 | 2026-09-04 | airflow-ti / airflow_debugger | **The debugger's real-log corpus cannot be refreshed after 2026-09-10 and nothing detects drift from production.** The acceptance capture's source falls out of the 7-day Cloud Logging window, and CI has no gcloud auth, so the vendored bytes can never be compared to prod. If the log shape changes again the corpus will keep certifying the old shape, which is the same failure mode it was built to prevent, one level up. Options: a scheduled job that re-captures into the repo, or a periodic manual refresh with a recorded date. | Raised by two independent refuters on AUDI-1327 and left unresolved there. | M | Malachi | idea | AUDI-1327 §8, [[project_airflow_debugger]] |
| IMP-117 | 2026-09-04 | airflow-ti / airflow_debugger | **43 corpus waivers warn past their review dates with nothing forcing the review.** Waiver expiry fails only when `DEBUGGER_WAIVER_EXPIRY=1` is set, and CI does not set it. The gate was deliberately soft so a date-triggered failure cannot block an incident hotfix on an unrelated PR, which is right, but it means the waiver list decays silently. A weekly job that reads the list and posts the expired ones would close it without blocking anyone. | AUDI-1327; the design choice and its cost were both flagged by refuters. | S | Malachi | idea | AUDI-1327 §8 |
| IMP-118 | 2026-09-04 | airflow-ti / spark_optimizer | **A half-failed sweep publishes a confident backlog with no error surfaced.** 6 of 14 published sweeps ran at 154 jobs instead of ~344 because 194 of 200 archive downloads failed; their headers read `(newest 6 of 200, 194 failed)` and nothing alerted. `phs.list_batches` returns `[]` on any `gcloud` error without raising (`phs.py:35-38`), the DAG catches the exception into `phs_n = 0` with only a `logger.warning`, and the empty-sweep guard tests the total rather than each half. `phs.fetch_logs` also still shells out to `gsutil cp -r` (`phs.py:110`) although gsutil is banned on Astro pods. Restoring those six sweeps moves mean fleet coverage from 28% to 39% with no design change. | AUDI-1329, measured across all 14 published sweeps 2026-08-21..09-03. | M | Malachi | idea | AUDI-1329 §2, [[project_airflow_optimizer]] |
| IMP-119 | 2026-09-04 | airflow-ti / spark_optimizer | **Raise `MAX_BATCHES`, `phs.list_batches(limit=)` and `MAX_BYTES` together, or the change is inert.** `MAX_BATCHES=150` re-binds at 195-201 eligible inside the 500-slice, so it and the list limit must move together; then `MAX_BYTES=4 GiB` (`phs.py:83`) binds, because 460 PHS dirs at a 4.4 MB mean is ~2.0 GiB against an archive half already charging 1.81 GiB to the same tmpdir. All three together take coverage to roughly 600/763 = 79%. | AUDI-1329. | M | Malachi | idea | AUDI-1329 §9, [[project_airflow_optimizer]] |
| IMP-120 | 2026-09-04 | airflow-ti / feature_store + bidstream_hourly | **An output-contract check is the only thing that catches the silent-failure class, and it belongs to neither existing tool.** 33 output partitions lost in 30 days behind runs that reported SUCCESS. Listing the `hh=`/`dt=` directory ~2h after the run and asserting `_SUCCESS`, gated on a non-empty source partition, measured 18 of 18 recall at 100% precision — the only one of five candidate sources that worked. Also re-raise (or emit a structured failure record) in the three swallowing loops: `site_network_hourly.py:246`, `aug_log_ip_hourly.py:202`, `aug_log_ip_vertical_id_hourly.py:288`. And settle what `_SUCCESS` means: neither `gcs_path_utils.filter_existing_gcs_paths` nor `writer.py:_filter_relpath` gates on it, so a partial partition reads as good. | AUDI-1329 §6; generalises IMP-104. | L | Malachi | idea | AUDI-1329 §6, [[project_airflow_debugger]] |
| IMP-121 | 2026-09-04 | airflow-ti / airflow_debugger | **Write detectors for the 8 residue classes; 24 of the 30 unmatched failures are detectable from artifacts already on disk.** In three classes `parse.py` already extracts the discriminating evidence and no signature consumes it (`pod_wait_seconds`/`pod_deleted`; `poke_target`/`poke_count`/`reschedule_count`; the missing-batch-id note). Moves 215/245 to ~239/245 = 98% with no new data source. Separately: 20 of the 48 keys have never matched a real log and the `engine` filter in `classify()` is dead because all 44 signatures carry `engine="any"`. | AUDI-1329 §5a. | M | Malachi | idea | AUDI-1329, [[project_airflow_debugger]] |
| IMP-122 | 2026-09-04 | airflow-ti / airflow_debugger | **The parse-rate canary has never worked.** Its threshold `max(2*norm, norm+0.25)` is unreachable above norm 0.5 and the norm sits at 0.62, so the debugger has no functioning self-check. Benign `upstream_failed` empty logs are what pushed the norm there; the empty-log denominator needs to exclude them and the threshold needs a form that stays reachable. | AUDI-1329; the canary shipped on PR 1260 and was never observed firing. | S | Malachi | idea | AUDI-1329, [[project_airflow_debugger]] |
| IMP-123 | 2026-09-04 | airflow-ti / spark_optimizer | **26% of the Spark fleet writes nothing the optimizer can read.** 195 Spark task instances/day on `DbxDbtOperator` (144.7), `ModelPysparkDbxJobOperator` (48) and `ModelPysparkWorkflowOperator` (2). No cap or retention change touches it; it needs `cluster_log_conf` on the Databricks job clusters and `DATABRICKS_PROFILE` set on the prod deployment. This is the largest single coverage block and the only item that changes another team's runtime. | AUDI-1329 §4. | M | Malachi | idea | AUDI-1329, [[project_airflow_optimizer]] |

