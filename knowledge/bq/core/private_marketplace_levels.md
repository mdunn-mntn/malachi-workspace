---
doc_type: bq_table
title: core.private_marketplace_levels
summary: "Tiny 3-row enum dim naming the scope at which a private_marketplace_group applies (global / advertiser / campaign_group); CDC dim, no partition."
dataset: core
table: private_marketplace_levels
object_type: VIEW
physical_table: dw-main-bronze.integrationprod.core_private_marketplace_levels
grain: "one row per PMP scoping level (3 rows: global / advertiser / campaign_group)"
partition_by: none
require_partition_filter: false
cluster_by: [id]
time_unit: milliseconds
ttl_days: null
approx_rows: 3
approx_logical_bytes: 246
schema_synced: 2026-07-19
last_verified: 2026-07-19
coverage_state: enriched
domain: [private_marketplace, mntn_select, dimensions]
keywords: [pmp, private marketplace, level, scope, global, advertiser, campaign group, enum lookup, cdc dimension]
source: INFORMATION_SCHEMA+human
tags: []
---

# core.private_marketplace_levels

## Purpose
Enum lookup that names the **scope at which a private-marketplace group applies**. A `private_marketplace_group` carries a `private_marketplace_level_id` FK into this table; the level says whether that PMP group is defined `global`ly, per `advertiser`, or per `campaign_group`. Reach for it only to decode `private_marketplace_group.private_marketplace_level_id` into a human-readable scope. Three seeded rows, effectively static since 2024-06-03.

## Grain & keys
- **Grain:** one row per PMP scoping level. Exactly 3 rows.
- **Primary key:** `id` (INT64). Values 1/2/3.
- **Referenced by:** `core.private_marketplace_groups.private_marketplace_level_id` → `id`.
- No `deleted` / `is_test` columns exist on this dim, so the usual `deleted=FALSE AND is_test=FALSE` live-row filter does not apply (and cannot be used).

<!-- AUTO:SCHEMA START — regenerated by scripts/bq_introspect.sh; do NOT hand-edit inside markers -->
| column | type | nullable | partition | cluster# |
|--------|------|----------|-----------|----------|
| id | INT64 | YES |  |  |
| name | STRING | YES |  |  |
| create_time | TIMESTAMP | YES |  |  |
| update_time | TIMESTAMP | YES |  |  |
| datastream_metadata | STRUCT<uuid STRING, source_timestamp INT64> | YES |  |  |
<!-- AUTO:SCHEMA END -->

## Column meanings (only the non-obvious ones)
- **`name`** — the level's scope label. Full domain (live `SELECT DISTINCT`, ordered by `id`): `1=global`, `2=advertiser`, `3=campaign_group`. These are the three levels at which a PMP group can be scoped.
- **`create_time` / `update_time`** — business seed time; all 3 rows are `2024-06-03 12:31:24` (the enum was seeded once and never edited). Use these for business time, not `datastream_metadata.source_timestamp`.
- **`datastream_metadata.source_timestamp`** — Datastream CDC-capture epoch in **milliseconds** (anchor: `TIMESTAMP_MILLIS(1766121457224) = 2025-12-19 05:17:37`; `TIMESTAMP_MICROS` yields 1970 = wrong). It is the capture/re-sync time, **not** business time — all 3 rows share the identical value (a full-table CDC re-snapshot), so it is useless for ordering or change detection here. Prefer `create_time`/`update_time`.

## Joins & relationships
- **`private_marketplace_groups.private_marketplace_level_id = private_marketplace_levels.id`** — partner grain: `private_marketplace_groups` is the **N side** (145 group rows as of 2026-07-19), this table is the **1 side** (3 rows). Joining **groups → levels is a safe N:1 decorate** (no fan-out — adds the level name to each group). Joining **levels → groups fans out 1:N** (one level maps to many groups) — do not start a join from this table unless you intend that fan-out.
- Part of the PMP dimension family: `private_marketplace_families`, `private_marketplace_groups` (references this table), `private_marketplace_deals`, `private_marketplace_deals_x_private_marketplace_groups`, `campaign_group_x_private_marketplace_deals`.

## Gotchas
- **Only `global` (id=1) is actually used.** As of 2026-07-19, all 145 `private_marketplace_groups` rows carry `private_marketplace_level_id=1`. Levels 2 (`advertiser`) and 3 (`campaign_group`) are seeded enum values with **zero referencing groups** — real but unused. Don't assume advertiser/campaign-group-scoped PMP groups exist in the data just because the enum defines them.
- **No soft-delete columns.** Unlike most CDC dims, there is no `deleted`/`is_test` here — every row is live; do not add that filter (it will error).
- **`datastream_metadata.source_timestamp` is uniform** across all rows (CDC re-sync artifact, ms epoch) — never treat it as a per-row change timestamp.
- Static seed data: `create_time == update_time` for all rows; do not expect this dim to change.

## Cost & partitioning notes
- Physical `dw-main-bronze.integrationprod.core_private_marketplace_levels` is a **TABLE** (CDC from Postgres), **not partitioned**, **clustered by `id`**. 3 rows / **246 bytes** total (`bq show` numBytes, whole table).
- No partition filter to apply — this is a trivial lookup. `SELECT *` of the whole table dry-runs at ~0 GB and bills tier-1 minimum (~0.01 GB billed). Cluster on `id` only helps point-lookups by id, irrelevant at this size.
- `approx_logical_bytes: 246` = real backing `numBytes` of the single physical table.

## Example queries
```sql
-- decode a PMP group's scope (safe N:1 lookup, no fan-out)
SELECT g.private_marketplace_group_id, g.name AS group_name, l.name AS level
FROM `dw-main-silver.core.private_marketplace_groups` g
LEFT JOIN `dw-main-silver.core.private_marketplace_levels` l
  ON g.private_marketplace_level_id = l.id;
```

## Observed cost
<!-- OBSERVED:COST START -->
<!-- perf-analyst appends dated one-liners here: `- YYYY-MM-DD: <slice> scanned <N> GB (est <M>), slot <S>s — <note>` -->
<!-- OBSERVED:COST END -->

## Observed facts
<!-- OBSERVED:FACTS START -->
<!-- capture/curator appends tribal findings here: `- YYYY-MM-DD: <fact verified against source>` -->
<!-- OBSERVED:FACTS END -->

## Changelog
<!-- CHANGELOG START -->
<!-- coverage transitions + schema changes: `- YYYY-MM-DD: skeleton→enriched` / `- YYYY-MM-DD: column X added` -->
- 2026-07-19: skeleton→enriched. Resolved physical (bronze.integrationprod.core_private_marketplace_levels, TABLE, 3 rows/246 B, no partition, cluster=id). Domain of name = global/advertiser/campaign_group. source_timestamp unit = milliseconds (anchor 2025-12-19). FK from private_marketplace_groups confirmed (N:1); only level 1=global used (145 groups). No prose oracle existed in data_catalog.md/data_knowledge.md beyond the silver.core inventory listing — enriched from live schema.
<!-- CHANGELOG END -->

## View definition
```sql
SELECT
  *
FROM
  `dw-main-bronze`.integrationprod.core_private_marketplace_levels
```
