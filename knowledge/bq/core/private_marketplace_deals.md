---
doc_type: bq_table
title: core.private_marketplace_deals
summary: "Small CDC dimension: one row per private-marketplace (PMP) deal config — exchange/SSP deal id, floor price, partner, pricing model, window, active flag. All rows are CTV (channel_id=8)."
dataset: core
table: private_marketplace_deals
object_type: VIEW
physical_table: dw-main-bronze.integrationprod.core_private_marketplace_deals
grain: "one row per private_marketplace_deal_id (PMP deal configuration)"
partition_by: none
require_partition_filter: false
cluster_by: [private_marketplace_deal_id]
time_unit: milliseconds
ttl_days: null
approx_rows: 1072
approx_logical_bytes: 239619
schema_synced: 2026-07-19
last_verified: 2026-07-19
domain: [pmp, deals, supply, inventory, ctv]
keywords: [private marketplace, pmp, deal, beeswax, exchange, floor price, publisher, ssp, pub_direct, partner, dsp]
source: INFORMATION_SCHEMA+human
tags: [dimension, cdc, lookup]
---

# core.private_marketplace_deals

## Purpose
Reference dimension for private-marketplace (PMP) deal configurations. One row per PMP deal MNTN
transacts on an exchange/SSP — the exchange-side deal id, its floor price, pricing model, partner,
publisher, active window, and channel. Reach for it to decode a `private_marketplace_id` /
`partner_deal_id` seen in impression or augmentor logs into a human deal name, or to answer "what deals
exist, on which exchange, at what floor, for which channel." Not built by MNTN — it is CDC-replicated
from the platform config DB (deals originate from Beeswax/exchanges). Tiny (~1,072 rows).

## Grain & keys
- **Grain:** one row per `private_marketplace_deal_id` (verified: 1,072 rows = 1,072 distinct
  `private_marketplace_deal_id`). No history/versioning — this is the current-state config row per deal.
- **Primary key:** `private_marketplace_deal_id` (also the physical cluster key).
- **Alternate key:** `partner_deal_id` is 1:1 with the PK (1,072 distinct) — the exchange-side deal id
  string used to match against `impression_log.private_marketplace_id` / augmentor-log PMP fields.
- **No `deleted` / `is_test` columns** on this dim (unlike most integrationprod tables) — do NOT add the
  standard `deleted=FALSE AND is_test=FALSE` filter; it will error. Liveness is expressed by `active` +
  the `start_time`/`end_time` window instead.

<!-- AUTO:SCHEMA START — regenerated by scripts/bq_introspect.sh; do NOT hand-edit inside markers -->
| column | type | nullable | partition | cluster# |
|--------|------|----------|-----------|----------|
| private_marketplace_deal_id | INT64 | YES |  |  |
| partner_deal_id | STRING | YES |  |  |
| user_id | INT64 | YES |  |  |
| floor_price | NUMERIC | YES |  |  |
| name | STRING | YES |  |  |
| partner_id | INT64 | YES |  |  |
| start_time | TIMESTAMP | YES |  |  |
| end_time | TIMESTAMP | YES |  |  |
| active | BOOL | YES |  |  |
| min_budget | NUMERIC | YES |  |  |
| max_budget | NUMERIC | YES |  |  |
| deal_type_id | INT64 | YES |  |  |
| create_time | TIMESTAMP | YES |  |  |
| update_time | TIMESTAMP | YES |  |  |
| channel_id | INT64 | YES |  |  |
| dsp_partner_id | INT64 | YES |  |  |
| audit_user_id | INT64 | YES |  |  |
| pricing_model | STRING | YES |  |  |
| partnership_deal_type | STRING | YES |  |  |
| private_marketplace_family_id | INT64 | YES |  |  |
| campaign_group_id | INT64 | YES |  |  |
| advertiser_id | INT64 | YES |  |  |
| private_marketplace_level_id | INT64 | YES |  |  |
| publisher_id | INT64 | YES |  |  |
| datastream_metadata | STRUCT<uuid STRING, source_timestamp INT64> | YES |  |  |
<!-- AUTO:SCHEMA END -->

## Column meanings (only the non-obvious ones)
- **`partner_deal_id`** — exchange/SSP-side deal id string; the value that shows up as `private_marketplace_id`
  on impression/augmentor rows. Join on this (not the internal `private_marketplace_deal_id`) to reconcile
  logs to config.
- **`advertiser_id` — empirically ALWAYS NULL (1,072/1,072).** Do NOT use this column to attribute a deal
  to an advertiser; it is not populated at the deal-config grain. See Joins for where the binding lives.
- **`campaign_group_id` — empirically NULL for 1,071/1,072 rows.** Same warning as `advertiser_id`; not the
  join path despite what older catalog prose implied.
- **`private_marketplace_family_id`** — groups deals into a family/bundle (21 distinct families; set on only
  94/1,072 rows, NULL otherwise). The advertiser/campaign-group binding happens at this family/group level,
  not per-deal (see Joins).
- **`private_marketplace_level_id`** — deal-scope enum; 1 = the ordinary per-deal level (1,071/1,072), 3 = one
  outlier row (rare higher-level scope). Effectively constant.
- **`floor_price`** (NUMERIC) — the deal's bid-floor CPM (exchange-side price floor); always populated. This
  is an inventory price floor, not an MNTN margin/take-rate.
- **`pricing_model`** (STRING) — how the deal is priced: `FIXED` (887), `BIDDABLE` (119), NULL (66).
- **`partnership_deal_type`** (STRING) — supply-path type: `PUB_DIRECT` (664, publisher-direct), `SSP` (166),
  `EXCLUSIVE` (153), NULL (89).
- **`deal_type_id`** — empirically constant = 2 for every row (single-value enum in current data).
- **`channel_id`** — empirically constant = 8 = **CTV** for every row (all PMP deals here are CTV/OTT; 8=CTV
  per the platform channel convention, 1=display).
- **`partner_id`** — exchange/SSP partner id (FK to a partners lookup; many values, top: 12, 13, 19, 61, 11…).
- **`dsp_partner_id`** — DSP partner id: 8 (836, the dominant Beeswax path), 79 (178), 2 (57), NULL (1).
- **`publisher_id`** — publisher FK; set on only 136/1,072 rows (mostly NULL — populated for publisher-direct deals).
- **`min_budget` / `max_budget`** (NUMERIC) — deal budget bounds; set on only 14/1,072 rows (almost always NULL).
- **`active`** (BOOL) — soft on/off flag: 416 TRUE / 656 FALSE. Not a delete; combine with the
  `start_time`/`end_time` window for true liveness.
- **`user_id` / `audit_user_id`** — platform user who owns / last edited the deal (FK to users; not analytics-relevant).
- **`create_time` / `update_time`** (native TIMESTAMP, UTC) — config create / last-modified; range 2021-12-15 → 2026-07-17.
  Prefer these for business time.
- **`datastream_metadata.source_timestamp`** — CDC-capture epoch in **MILLISECONDS** (resolved: `TIMESTAMP_MILLIS(source_timestamp)`
  == `update_time` exactly; `TIMESTAMP_MICROS` yields 1970). It is CDC replication time, NOT business time — use
  `update_time`/`create_time` instead. `datastream_metadata.uuid` = CDC row uuid.

## Joins & relationships
- **PMP deal ↔ campaign group (the real path):** NOT via this table's `campaign_group_id`/`advertiser_id`
  (both effectively empty). The binding lives one level up:
  - `core_private_marketplace_groups` (145 rows) — the deal-group/family level; **this** table carries the
    populated `campaign_group_id` + `advertiser_id` + budgets + `pmp_threshold_ceiling`/`pmp_threshold_floor`.
  - `core_campaign_group_x_private_marketplace_deals` (junction, 608 rows) — maps `campaign_group_id` ↔
    `private_marketplace_deal_group_id` (the group/family, **not** the individual `private_marketplace_deal_id`).
    Junction grain is deal-group × campaign-group (N:M), so joining a campaign group to its deals fans out;
    dedup on the group id.
  - This deal table relates to the group level via `private_marketplace_family_id` (NULL on ~91% of rows).
- **PMP deal ↔ impression/augmentor logs:** join `partner_deal_id` (here) → `private_marketplace_id`
  (`impression_log` / augmentor PMP fields). Impression side is 1:N (one deal → many impressions); huge
  fan-out — filter the log side by date first.
- **`partner_id` → partners lookup**, **`publisher_id` → publishers**, **`user_id`/`audit_user_id` → users** —
  each is a small dim FK (this side N:1); low fan-out risk.
- **`deal_type_id` / `private_marketplace_level_id` / `channel_id`** — enum FKs to tiny type tables; in current
  data they are single-valued so the join is cosmetic.

## Gotchas
- **`advertiser_id` is 100% NULL and `campaign_group_id` is 99.9% NULL** — the single most important trap.
  Older catalog prose ("PMP deal → Campaign Group via campaign_group_id") describes columns that are not
  actually populated here. Get advertiser/campaign-group context from `core_private_marketplace_groups` +
  the junction table instead.
- **No soft-delete columns** — `deleted`/`is_test` do not exist; the standard integrationprod filter errors.
- **`active` is not liveness by itself** — a deal can be `active=TRUE` outside its `start_time`/`end_time`
  window; check both.
- **`deal_type_id`, `channel_id` are constant** (2 and 8) in the current snapshot — do not read distinctions
  into them; all deals here are CTV.
- **`datastream_metadata.source_timestamp` is milliseconds and is CDC time, not business time** — using it as
  a business timestamp mis-dates deals; use `update_time`.
- **`name` is free text from the exchange** — filter with `LOWER(name) LIKE '%...%'` for themed inventory
  (e.g. `'%nba%'` for sports); no controlled vocabulary.

## Cost & partitioning notes
- **Unpartitioned, tiny dimension** — no partition trap. Physical `core_private_marketplace_deals` is ~1,072
  rows / ~234 KB, clustered on `private_marketplace_deal_id`. A full `SELECT *` here billed the 10 MB minimum
  (dry-run showed 0 GB processed / 0.01 GB billed) — scanning the whole table is free-tier cheap.
- **The one filter that matters is on the JOIN partner, not this table** — when joining to impression/augmentor
  logs, always bound the log side by its partition date; the deals dim itself needs no filter.
- Avoid `SELECT datastream_metadata` in CSV exports (STRUCT breaks flat CSV) — select scalar columns.

## Example queries
```sql
-- decode themed PMP inventory (sports deals), current-state config
SELECT private_marketplace_deal_id, partner_deal_id, name, floor_price,
       pricing_model, partnership_deal_type, active, start_time, end_time
FROM `dw-main-silver.core.private_marketplace_deals`
WHERE LOWER(name) LIKE '%nba%'
ORDER BY active DESC, name;

-- advertiser/campaign-group binding is at the GROUP level, not this table
SELECT g.campaign_group_id, g.advertiser_id, g.name AS group_name
FROM `dw-main-silver.core.private_marketplace_groups` g   -- carries the populated FKs
WHERE g.advertiser_id = <AID>;
```

## Observed cost
<!-- OBSERVED:COST START -->
<!-- perf-analyst appends dated one-liners here: `- YYYY-MM-DD: <slice> scanned <N> GB (est <M>), slot <S>s — <note>` -->
<!-- OBSERVED:COST END -->

## Observed facts
<!-- OBSERVED:FACTS START -->
<!-- capture/curator appends tribal findings here: `- YYYY-MM-DD: <fact verified against source>` -->
<!-- OBSERVED:FACTS END -->

## Changelog
<!-- CHANGELOG START -->
- 2026-07-19: skeleton→enriched. Confirmed physical = unpartitioned TABLE (1,072 rows / 239,619 bytes), cluster=[private_marketplace_deal_id], no deleted/is_test cols. Grain 1:1 on private_marketplace_deal_id (and partner_deal_id). Resolved datastream_metadata.source_timestamp = MILLISECONDS (== UNIX_MILLIS(update_time)). Enum domains derived from live SELECT DISTINCT: channel_id≡8 (CTV), deal_type_id≡2, pmp_level_id 1/3, pricing_model FIXED/BIDDABLE/NULL, partnership_deal_type PUB_DIRECT/SSP/EXCLUSIVE/NULL. **Drift reconciled:** data_catalog prose lists advertiser_id/campaign_group_id as the deal→group join, but both are empirically NULL (100% / 99.9%) — real binding is core_private_marketplace_groups + core_campaign_group_x_private_marketplace_deals junction (keyed on private_marketplace_deal_group_id, not the deal id).
<!-- CHANGELOG END -->

## View definition
```sql
SELECT
  *
FROM
  `dw-main-bronze`.integrationprod.core_private_marketplace_deals
```
