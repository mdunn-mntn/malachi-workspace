---
doc_type: bq_table
title: <dataset>.<table>
summary: "one row per <grain> — <what this table is for in ≤12 words>"
dataset: <dataset>
table: <table>
object_type: BASE TABLE
physical_table: self
grain: "one row per <entity/event/day>"
partition_by: <partition_column_or_unknown>
require_partition_filter: unknown
cluster_by: []
time_unit: unknown
ttl_days: null
approx_rows: null
approx_logical_bytes: null
schema_synced: null
last_verified: null
coverage_state: skeleton
domain: []
keywords: []
source: INFORMATION_SCHEMA+human
tags: []
---
<!--
  Front-matter notes (do NOT put inline `#` comments on list-valued lines — cluster_by / domain /
  keywords / tags — the index parser would mis-read them):
  · object_type      BASE TABLE | VIEW | MATERIALIZED VIEW
  · physical_table   'self' for base tables; the sqlmesh__* table for a view (recover partition/cluster/TTL there)
  · partition_by     a resolved column | none | unknown   (VIEW default = unknown, an actionable gap)
  · cluster_by       SOURCE ORDER IS MEANINGFUL — never reorder; the index preserves it
  · time_unit        epoch unit of the INT time column: ns | us | ms | s | timestamp | na | unknown
                     (the epoch column's unit e.g. microseconds — NOT partition granularity; DAY/HOUR
                      belong in the physical_table note + cost section, never here)
  · schema_synced    MACHINE date — bumped by bq_introspect on AUTO:SCHEMA regen
  · last_verified    HUMAN date — set ONLY when prose is confirmed vs source; null until then
  · coverage_state   skeleton | enriched | verified
  · domain           topic buckets → bq/_TOPICS.md      · keywords → _ROUTING.md (both order-independent)
-->

# <dataset>.<table>

## Purpose
<Why this table exists and when to reach for it. 2–4 sentences. Human-written.>

## Grain & keys
- **Grain:** one row per <…>.
- **Primary/natural key:** `<col(s)>`.
- **Foreign keys / join columns:** `<col>` → `<other_table>.<col>`.

<!-- AUTO:SCHEMA START — regenerated by scripts/bq_introspect.sh; do NOT hand-edit inside markers -->
| column | type | nullable | partition | cluster# |
|--------|------|----------|-----------|----------|
| _(run `scripts/bq_introspect.sh <dataset>` to populate)_ | | | | |
<!-- AUTO:SCHEMA END -->

## Column meanings (only the non-obvious ones)
> Don't restate the type. Explain what a column *means*, its units, encodings, and traps.
- `<col>` — <meaning, units, enum values, "-1 means unknown", etc.>

## Joins & relationships
- <How this table connects to others; fan-out warnings; which side is the many.>

## Gotchas
- <Late-arriving data? Duplicates? Timezone of the partition column? Soft-deletes? Nulls that mean something?>

## Cost & partitioning notes
- **Always filter** `<partition_column>` (type/timezone/grain: <…>). `require_partition_filter: <…>`.
- Clustered on `<cols>` — filtering those columns (leading key first) prunes blocks within a partition.
- Typical scan cost for a full day: ~<N> GB. Avoid `SELECT *` (this table is <wide/narrow>).

## Example queries
```sql
-- Canonical, partition-pruned example
SELECT <cols>
FROM `<GCP_PROJECT>.<dataset>.<table>`
WHERE <partition_column> BETWEEN '<start>' AND '<end>'
```

## Observed cost
<!-- OBSERVED:COST START -->
<!-- perf-analyst appends dated one-liners here: `- YYYY-MM-DD: <slice> scanned <N> GB (est <M>), slot <S>s — <note>` -->
<!-- OBSERVED:COST END -->

## Observed facts
<!-- OBSERVED:FACTS START -->
<!-- capture/curator appends tribal findings here: `- YYYY-MM-DD: <fact verified against source>` -->
<!-- OBSERVED:FACTS END -->

## Changelog
<!-- CHANGELOG START -->
<!-- coverage transitions + schema changes: `- YYYY-MM-DD: skeleton→enriched` / `- YYYY-MM-DD: column X added` -->
<!-- CHANGELOG END -->
