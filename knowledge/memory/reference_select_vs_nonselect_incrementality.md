---
name: reference_select_vs_nonselect_incrementality
description: "AUDI-1172: Select prospecting ~5x more incremental than non-Select (PTV) — +22.0% vs +4.3% rel visit lift pooled, 27/35 advertisers favor Select (ghost-bid, 2026-07)"
metadata: 
  node_type: memory
  type: reference
  originSessionId: ab62c6c6-93ab-41d7-aee9-578a3fb85ad7
doc_type: memory
keywords: [select vs nonselect, incrementality, audi-1172, ghost bid, visit lift, product_id, lift__ghost_bid_rollup, ptv, prospecting, kirsa]
domain: [incrementality, experimentation]
lifecycle: active
last_verified: 2026-07-28
---
**AUDI-1172 (2026-07-28, Kirsa's Select Performance Analysis):** for advertisers running both, **MNTN Select prospecting is ~5x more incremental than non-Select (PTV) prospecting.** Pooled across 35 advertisers running both (ghost-bid holdout, 2026-06-22→07-27, prospecting): **Select +22.0% rel visit lift** (treated 2.39% vs holdout 1.19%) vs **non-Select +4.3%** (1.38% vs 1.21%), both p<.05, near-identical holdout baselines. Robust per-advertiser: Select wins **27/35**, median edge +46pp; largest all confirm (Zazzle +95/+5, Hugo +84/+2, Planned Parenthood +142/+11, Upneeq +79/+25).

**This is the per-advertiser Select-vs-non-Select cut TI-933 said was "gated on ghost-bidder" — now unblocked** because `lift__ghost_bid_*` is queryable. Method: `dw-main-gold.reporting.lift__ghost_bid_rollup` (level=campaign_group) × `campaign_groups.product_id` (2=Select/1=PTV), clean-gate `se>0 AND NOT low_coverage`, IVW-pool CGs → advertiser×product (weight 1/se²; NOT count pool). Rel = IVW abs / holdout VR.

**Gotchas:** ghost-bid ITT is **bid-grain** (win-rate diluted ~10%) → compare RELATIVE lift, never the absolute pp. Data floor **2026-06-22** (no backfill). **LEG CONFOUND (corrected 2026-07-29, supersedes "Beeswax only / Rust not folded in"): both legs are now in the lift tables — Select is ~97% the MNTN Rust leg (partner 79), non-Select ~100% Beeswax (partner 8).** Select is measured on the Rust CLEAN-reference leg; non-Select on the Beeswax leg (residual `ghost_frac` multiplicity bias, skews negative) — so part of the +22% vs +4% gap may be measurement-leg, not product. Confirm same-leg before treating the gap as pure product effect. Coverage of Kirsa's 93 AIDs: 74 have clean data, 35 both, 8 Select-only, 31 non-Select-only. All rows objective_id=1 (holdout is prospecting-only by construction). Deliverable: xlsx in `My Drive/Tickets/AUDI-1172/`. **The 7-day window is short vs the 30-45d advertiser-standard attribution → the +22% is a conservative FLOOR** (true effect over an advertiser's horizon is larger; don't compare head-to-head with advertiser-attributed numbers). Can't widen yet: only ~35d of ghost-bid data (breaks a matured 30d window) + variable/longer windows break cross-product comparability. Views accumulate (no TTL) → **re-measure Select vs non-Select on a matured 30d window ~Sept 2026** (expect a bigger gap). "user"=IP≈household; window is fixed 7d from each IP's first bid, not the full period.

**CPIV/CPIA follow-up (Kirsa, 2026-07-29) — first-pass computed, DO-NOT-PUBLISH yet; path now RESOLVED after Matt call.** Pooled `total_spend / incremental_visits` = Select ~$6 vs non-Select ~$30 CPIV (directional only). **Matt call 2026-07-29 unblocked the two open items:** (a) LEG — no confound and no spend restriction: every campaign_group is entirely one bidder (Select→Mountain/Rust p79, PTV→Beeswax p8, never split), so all_facts spend maps cleanly to a CG's single leg; the leg IS the product split. (b) SPEND/VV BASIS — Matt tracks NO spend (no CIL) and his "visit" = pixel-fire within 7d of an IP's first bid, not a Reporting Verified Visit, so raw all_facts spend / his incremental_visits over-counts. Bridge (both computable now): households reached ≈ `ip_compliance × n_treatment` (~50% win) for cohort-matched spend; and `all_facts.first_day_visits…seventh_day_visits` = Matt's exact 7d window, so WE can measure the pipeline→Verified-Visit scaling factor Matt says he lacks (compare `_results.vis_treatment` to all_facts 7d attributed visits, same CGs). Per-advertiser CPIV is unstable (~half explode/negative net incremental → publish pooled only, Select cheaper in just 15/28 → the pooled advantage is concentration-driven). CPIA is pooled-only (39/43 Select advertisers <20 holdout conversions). Denominator confirmed = raw-count `incremental_visits` (Matt). Transcript: `tickets/audi_1172_.../meetings/audi_1172_01_matt_brorby_spend_scaling_2026_07_29.txt`.

**CPIV RECONCILED (2026-07-29, obj=1-matched metered spend): Select $6.01 vs non-Select $30.47 pipeline-visit basis (5.1x cheaper) → $21.21 vs $36.53 Verified-Visit basis (1.7x).** The edge is BASIS-DEPENDENT: pipeline→VV factor k=0.28 Select (CTV, sparse attribution) vs 0.83 non-Select (display), so the "5x cheaper" headline compresses to ~1.7x on the basis advertisers see. Spend method: join metered `all_facts` spend on `campaign_group_id` scoped to `objective_id=1` (NOT reconstructed; Matt's compliance path reduces to the same metered number). Scope gotcha: MUST filter obj=1 — non-Select CGs also run obj 5/6 (MT-S2/S3, ~$0.6M) that the lift never measured (all-obj inflates non-Select $30→$33). Validated: cost/household ≈$0.11 both, ip_compliance 0.59/0.44 (Matt's ~50% band). Solid = pipeline basis; VV basis pending Matt's confirm on the day-bucket-as-VV column + the k=0.83 display value. Artifacts: `queries/audi_1172_cpiv_reconcile.sql`, `artifacts/audi_1172_cpiv_compute.py`, `outputs/audi_1172_cpiv_pooled.csv`. **Shipped-deliverable defect to fix: the Headline shows count-pooled treated/holdout RATES (imply ~+100% Select lift) next to the IVW abs-lift (+22%) — inconsistent estimators side by side.** Full open-question list + method: `experimentation.md` §Ghost-bid lift; ticket §8. See [[reference_ghost_bid_lift_register]], [[reference_mntn_campaign_stages]], [[project_bidder_level_ghost_bidding_approved]].
