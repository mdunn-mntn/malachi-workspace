# Data output files — don't commit raw data to GitHub
*.csv
*.csv.gz
*.tsv
*.xlsx
*.xls
*.parquet
*.json

# Video / audio recordings
*.mp4
*.m4a
*.mov
*.avi
*.vtt
*.m4v

# Log files
*.log

# Binary/media files
*.docx
*.doc
*.pdf
*.png
*.jpg
*.jpeg
*.gif
*.svg
*.pptx

# Python / notebook artifacts
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.swp

# Secrets / credentials
.env
*.key

# Slack bot raw data — daily JSON dumps, not for remote
knowledge/slack_raw/
slack_bot/logs/

# Personal notes — not for remote
self_review/
tickets/ti_650_stage_3_vv_audit/outputs/ti_650_trace_table_24adv.json
tickets/ti_650_stage_3_vv_audit/outputs/ti_650_trace_table_amsoil.json
tickets/ti_832_feature_store_roas_cpa/outputs/ti_832_training_data.csv
tickets/**/meetings/*.m4a
*.zip

# AI Workflow Kit transient state
knowledge/bq/perf_archive/
knowledge/_staging/
.claude/.session_state

# Request-mining telemetry — LOCAL ONLY, keyword-only, never pushed (self-improvement kernel)
knowledge/.request_log.jsonl

# Spark / Databricks write-markers (never useful to track)
_SUCCESS
_started_*
_committed_*
_temporary/

# BQ run-capture noise
*.err

# Claude session/memory tree — belongs in global ~/.claude, never in-repo
.claude/projects/

# Vendored Todoist MCP tool — kept on disk for the MCP server, not part of the analytics repo
todoist-mcp-transfer/

# Licensed MNTN brand kit (paid Neue Haas fonts, logos) — local only, never committed
documentation/mntn_assets/

# workflow-audit working file (regenerated each run; reasoned report is committed)
claude-prompts/workflow_audits/signals_latest.md
claude-prompts/workflow_audits/.structure_findings.json
