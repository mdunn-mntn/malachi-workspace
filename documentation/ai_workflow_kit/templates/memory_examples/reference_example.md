---
name: reference_example
description: "Example reference memory — shows the front-matter contract for a pointer to an external resource"
metadata:
  node_type: memory
  type: reference
doc_type: memory
keywords: [example, reference, pointer, external resource, dashboard, link, template]
domain: [workflow]
lifecycle: active
last_verified: 2025-01-01
---
This is an example `reference` memory. Use it as a template for capturing **pointers to external
resources** — a dashboard URL, a ticket, a doc, a shared drive path. Delete it once you have real ones.

**What it points to:** <the resource> at `<url-or-path>`.

**When to reach for it:** <the recurring task where this resource is the answer>.

Keep reference memories to a single resource each. If a resource moves, update this file (that is what
`last_verified` tracks) rather than leaving a stale pointer. Link related memories with `[[their-name]]`.
