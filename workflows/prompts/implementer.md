# Implementer prompt — provenance stub

`../../.claude/agents/implementer.md` is the canonical, runnable version of this role — edit **that**
file. This stub is kept only for provenance (the Bun-derived prompt lineage). Roster + dispatch order:
`../agent_pass_runbook.md` → "Runnable roster."
