# Fixer prompt — provenance stub

`../../.claude/agents/fixer.md` is the canonical, runnable version of this role — edit **that** file.
This stub is kept only for provenance. Dispatch order: `../agent_pass_runbook.md` → "Runnable roster."
