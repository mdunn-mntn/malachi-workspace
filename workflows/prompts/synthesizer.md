# Synthesizer prompt — provenance stub

`../../.claude/agents/synthesizer.md` is the canonical, runnable version of this role — edit **that**
file. This stub is kept only for provenance. It runs once as a barrier at the end; see
`../agent_pass_runbook.md` → "Runnable roster."
