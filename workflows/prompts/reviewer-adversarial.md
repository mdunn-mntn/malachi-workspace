# Adversarial reviewer prompt — provenance stub

`../../.claude/agents/reviewer-adversarial.md` is the canonical, runnable version of this role — edit
**that** file. This stub is kept only for provenance. Two run in parallel per unit, each handed only
the doc + its source; see `../agent_pass_runbook.md` → "Runnable roster."
